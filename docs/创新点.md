# 创新点

> 创建日期: 2026-05-13
> 相关文档: [指令.md](指令.md) | [创新点.md](创新点.md) | [改进位置.md](改进位置.md) | [论文级创新点.md](论文级创新点.md) | [实验方案.md](实验方案.md) | [实验记录.md](实验记录.md) | [模块清单.md](模块清单.md) | [RT-SFOD训练策略.md](RT-SFOD训练策略.md)
> 说明: 每次改进在文档末尾追加。同日改进不加新日期头，不同日期改进先加 `## YYYY-MM-DD` 再追加内容。

---

## 2026-05-13

### 新增11个注意力模块

在双模态YOLO中融合架构中，于P3/P4/P5三个尺度的Concat融合之后插入注意力模块，用于对融合特征做跨通道/空间重标定。

| 模块 | 论文 | 类型 | 参数量 |
|------|------|------|--------|
| **Att_CBAM** | CBAM (ECCV 2018) | 通道+空间 | +0.05M |
| **Att_Coord** | Coordinate Attention (CVPR 2021) | 通道+坐标编码 | +0.03M |
| **Att_SimAM** | SimAM (ICML 2021) | 无参数3D | +0.00M |
| **Att_CPCA** | CPCA (2023) | 通道先验+多尺度空间 | +0.70M |
| **Att_EMA** | EMA (2023) | 分组交叉空间 | +0.06M |
| **Att_ECA** | ECA-Net (CVPR 2020) | 1D卷积通道 | +0.00M |
| **Att_Shuffle** | ShuffleAttention (2021) | 分组通道+空间 | +0.00M |
| **Att_LSKA** | LSKA (2023) | 大核可分离空间 | +0.41M |
| **Att_Triplet** | Triplet Attention (2020) | 跨维度空间 | +0.00M |
| **Att_GAM** | GAM (2021) | 全局注意力 | +9.83M |
| **Att_ELA** | ELA (2024) | 方向局部注意力 | +0.39M |

**插入策略**: 在双模态Concat融合之后(P3/P4/P5)，SPPF之前。注意力模块学习两种模态特征的互补性权重，抑制融合噪声。

**改进动机**: 双模态特征直接Concat存在信息冗余和模态冲突，注意力机制能自适应地筛选融合后的有效特征，在不同尺度上独立学习可见光-红外的重要性分布。

### 先前已集成的模块（7个）

| 模块 | 来源 | 核心思想 |
|------|------|----------|
| **ACBlock** | ACNet (ICCV 2019) | 非对称卷积，训练时3×3→3×3+3×1+1×3，推理BN融合零开销 |
| **BlazeBlock** | BlazeFace (CVPR 2019) | dw+pw-linear无激活瓶颈，轻量化IR分支 |
| **ASPP_Branch** | DeepLab | 不同膨胀率深度可分离卷积并行，多尺度上下文 |
| **GCBlock** | Context Gating (2017) | 跨通道特征重标定，可学习权重矩阵+BN+sigmoid |
| **C2f_DCN** | DCNv2 (CVPR 2019) | 可变形卷积v2，自适应采样+调制掩码 |
| **SEAttention** | SENet (CVPR 2018) | 全局池化→FC→ReLU→FC→Sigmoid通道重标定 |
| **CSP_ATT** | CSPNet + SE | SPPF替代方案，CSP+多尺度MaxPool+SE |

### 验证脚本

创建 `val_zjy_full.py`，输入模型权重目录即可输出 Precision/Recall/mAP50/mAP50-95/FLOPs/Params/FPS(含Preprocess/Inference/Postprocess/Total Time)，并追加到CSV。

### 验证结果

- 全部11个新注意力模块模型可成功实例化并完成forward pass
- Att_SimAM在FLIR数据集上完成1 epoch训练验证，无错误

---

## 2026-05-14

### 新增AIFI (RT-DETR Transformer自注意力)

从 `yolo-improve` 目录中筛选，将 RT-DETR 的 AIFI 模块集成到双模态YOLO中，替代 SPPF 层。

| 模块 | 论文 | 类型 | 参数量 |
|------|------|------|--------|
| **Att_AIFI** | RT-DETR (2023) | Transformer自注意力+2D位置编码 | +2.33M |

**改进动机**: SPPF 仅通过固定大小的池化核捕获上下文，无法建模全局长程依赖。AIFI 使用多头自注意力+2D sin-cos位置编码，在 P5 最深特征图上提供全局上下文建模能力，对双模态融合后的特征做跨空间位置的信息交互。

**插入位置**: P5 Concat融合之后，C2PSA之前（替代SPPF），其余结构不变。

**验证结果**: Att_AIFI 模型成功实例化（17.34M params）并通过 forward pass。

### 训练脚本

创建 `train_RGBT_midfuse_testAddModules_withResults.py`，训练完成后自动运行验证并输出 Precision/Recall/mAP50/mAP50-95/FLOPs/Params/FPS 到控制台和 CSV。

### yolo-improve 筛选分析

从 yolo-improve 的 20+ 个改进点中筛选：
- **纳入**: AIFI (全局自注意力)
- **跳过**: CAM(与ASPP重叠), IoU变体(损失函数改动), aLRPLoss(侵入性强), 各种Backbone替换(不在本阶段范围)
- **待评估**: ScalSeq (ASF-YOLO的3D卷积多尺度融合，已实现并验证通过)

### 新增Plug-and-play模块 (4个)

从 `Plug-and-play module` 目录中筛选28个模块，集成4个不重叠且有涨点潜力的模块。

| 模块 | 论文 | 类型 | 参数量 |
|------|------|------|--------|
| **Att_AFF** | AFF (WACV 2021) | 双输入注意力特征融合 | +3.38M |
| **Att_iAFF** | iAFF (WACV 2021) | 迭代注意力特征融合 | +4.95M |
| **Att_ULSAM** | ULSAM (2020) | 超轻量子空间注意力 | +0.01M |
| **Att_StripPool** | SPNet (CVPR 2020) | 1D条带池化长程空间上下文 | +0.28M |

**AFF/iAFF 创新点**: 替代简单的Concat操作，用局部+全局注意力学习可见光与红外特征的逐通道融合权重。这是**唯一专门为特征融合设计的模块**。

**插入策略**:
- AFF/iAFF: 替换P3/P4/P5的Concat，直接融合双模态特征
- ULSAM: Concat之后插入，分组空间softmax注意力
- StripPool: P5 Concat之后替代SPPF，条带池化捕获方向性长程依赖

**验证结果**: 全部4个模块成功实例化并通过forward pass。

---

## 2026-05-19

### 新增CMFADet模块 (3个YAML配置, 4个底层模块)

从 CMFADet (RGB-Infrared 航空目标检测论文) 中整合了4个互补模块。

| 模块 | 论文 | 类型 | 参数量 |
|------|------|------|--------|
| **Att_CIFusion** | CMFADet | 跨模态交叉交换通道注意力融合 | +5.7M |
| **Att_SFEM** | CMFADet | 空间-频率增强 (FFT频域+Scharr边缘), RGB专用 | +0.3M |
| **Att_IRAFAB** | CMFADet | 自适应多核深度卷积+门控线性单元, IR专用 | +0.3M |
| **Conv_DE** | DEConv | 5路边缘感知重参数化卷积, 推理零开销 | +0M |

**核心创新**:
- **CIFusion**: 不是简单Concat或加权融合。它做通道注意力门控后**交叉交换**RGB和IR的特征——RGB的注意力特征放到IR通道位置，IR的放到RGB位置，实现双向信息传递
- **SFEM + IR_AFAB**: 首次在YOLO中实现**非对称骨干**——RGB分支用频域+边缘分析，IR分支用自适应多核深度卷积
- **Conv_DE**: 训练时5种边缘差分卷积并行，推理时融合为1个3×3卷积，零推理开销

**YAML配置** (2026-05-19/):
| 文件 | 内容 |
|------|------|
| `CIFusion.yaml` | Concat → CIFusion (交叉交换融合) |
| `SFEM_IRAFAB.yaml` | RGB: SFEM + IR: IRAFAB (非对称骨干) |
| `CMFADet.yaml` | SFEM + IRAFAB + CIFusion (全组合) |

**验证结果**: 全部通过模型实例化和forward pass

### 新增ICAFusion模块 (1个YAML配置, 核心: TransformerFusionBlock)

从 ICAFusion (Iterative Cross-Attention Guided Feature Fusion, Pattern Recognition 2023) 中集成其核心跨模态Transformer融合模块。

| 模块 | 论文 | 类型 | 参数量 |
|------|------|------|--------|
| **Att_ICAFusion** | ICAFusion (PR 2023) | 迭代交叉注意力Transformer融合 | +16.6M (vs baseline Concat) |

**核心创新**:
- **双交叉注意力**: RGB和IR各用独立QKV投影，计算交叉注意力矩阵（IR query × VIS key / VIS query × IR key），实现双向信息传递
- **可学习系数残差**: 8个可学习系数控制注意力输出和MLP的残差比例，提供细粒度梯度调节
- **位置编码**: 可学习的1D位置编码，让Transformer感知特征图空间结构
- **自适应池化**: avg+max双池化可学习加权融合，将特征下采样到固定网格(16×16)降低计算量

**与CIFusion的差异**: CIFusion是轻量级通道注意力交叉交换(0.6M)，ICAFusion是完整的Transformer跨模态融合(16.6M)。前者更轻量适合浅层，后者全局交互能力更强适合深层。

**YAML配置** (2026-05-19/):
| 文件 | 内容 |
|------|------|
| `yolo26-RGBT-midfusion-ICAFusion.yaml` | Concat → Att_ICAFusion (迭代交叉注意力Transformer融合) |

**验证结果**: 模型成功实例化(58.82M params)并通过forward pass

### 训练/检测脚本合并重构

消除功能重复的脚本，用 argparse 统一接口：
- 删除 `train_AddModules_resume.py`（合并到 `train_AddModules.py --resume`）
- 删除 `train_resume.py`（功能覆盖）
- 删除 `val_zjy.py`（`val_zjy_full.py` 子集）
- 删除 `detect-1C/4C/6C/multispectral.py`（合并到 `detect.py --use_simotm --channels`）
- 增强 `train_AddModules.py` 支持 `--resume` 断点续训
- 增强 `detect.py` 支持 argparse 覆盖 8 种模态
- `train_RGBT_mine_print.py` 添加详细中文注释

---

## 2026-05-26

### 新增 ATAH 检测头 (Adaptive Task-aware Alignment Head)

从 CMFADet 中移植 ATAH 模块，替换标准 Detect 检测头。

| 模块 | 论文 | 类型 | 参数量 |
|------|------|------|--------|
| **Detect_ATAH** | CMFADet | 任务感知对齐检测头 | +3.2M (vs standard Detect) |

**核心创新**:
- **任务感知特征调制 (TaskAwareFeatureModulator)**: 全局平均池化→降维→Sigmoid门控，分离分类和回归分支的特征偏好
- **回归对齐 (DyDCNv2)**: 可变形卷积v2根据特征本身学习采样偏移，动态校正回归空间位置
- **分类对齐 (空间概率图)**: 学习空间注意力概率图约束分类输出，抑制背景噪声
- **共享特征提取**: P3/P4/P5 共享 share_conv 而非独立卷积分支，减少参数量
- **1×1 通道投影**: 将 P3/P4/P5 不同通道统一对齐到 hidc，无需修改 Neck

**移植改动 vs 原版 CMFADet**:
- `mmcv.ModulatedDeformConv2d` → `torchvision.ops.deform_conv2d`（消除mmcv依赖）
- 新增 1×1 投影层（原版依赖YAML统一通道，本项目Neck通道不一致）

**插入位置**: 替换 YOLO 最后一层 Detect，层号不变。P3/P4/P5 输入经 1×1 投影对齐后进入 ATAH。

### CMFADet 消融实验配置

在 `2026-05-19/CMFADet=SFEM+IRAFAB+CIF+ATAH/` 下创建 8 个 YAML 配置，系统验证 CMFADet 各模块贡献：

| 编号 | 配置 | SFEM | IR_AFAB | CIFusion | ATAH |
|------|------|:----:|:-------:|:--------:|:----:|
| Baseline | `baseline` | | | | |
| (a) | `a-SFEM` | ✓ | | | |
| (b) | `b-IRAFAB` | | ✓ | | |
| (c) | `c-CIF` | | | ✓ | |
| (d) | `d-ATAH` | | | | ✓ |
| (e) | `e-SFEM+IRAFAB` | ✓ | ✓ | | |
| (f) | `f-SFEM+IRAFAB+CIF` | ✓ | ✓ | ✓ | |
| (g) | `g-SFEM+IRAFAB+CIF+ATAH` | ✓ | ✓ | ✓ | ✓ |

**实验设计**: 所有 YAML 层号完全一致（替换模块不增减层），唯一变量是模块名称，确保对比严谨。

**验证结果**: 全部 8 个 YAML 成功通过模型实例化

---

## 2026-05-27

### 大规模模块集成 (来自 objectdetection_script 扫描)

Brief: 系统扫描 objectdetection_script 仓库的 module-info/ (30篇顶会)、yolo-improve/ (剩余模块)、objectdetection-tricks/ (16个技巧)，分四批集成。

### Batch 1: yolo-improve 模块 (10个)

| 模块 | 来源 | 机制 | 参数量 | RGBT价值 |
|------|------|------|--------|----------|
| Conv_Coord | CoordConv | 附加x/y坐标通道 | ~0 | 空间对齐双模态基线 |
| PConv / Faster_Block | FasterNet | 1/4通道3x3+恒等 | 标准30% | 极致轻量颈部 |
| C3_RFEM (TridentBlock) | RFEM | 共享权重3空洞率(1,2,3) | ~0额外 | 零参数多尺度 |
| StarBlock | StarNet CVPR2024 | 逐元素乘法隐式高维 | ~0额外 | 零成本跨模态高阶交互 |
| Dynamic_conv2d | DynamicConv | K=4专家核+温度softmax路由 | 标准4x | 模态自适应卷积 |
| EVCBlock | EVC | LVC可学习码本+LightMLP | ~400K | 双模态原型学习 |
| ContextAggregation | ContextAgg | 非局部全局上下文聚合 | ~8K | 场景级跨模态感知 |
| CARAFE | CARAFE | 内容感知重组核上采样 | ~20K | 对齐异构模态空间 |
| ASF_fusion | ASF | Zoom_cat+双重注意力 | ~5K | 跨尺度注意力融合 |
| DecoupledHead | DecoupledHead | 分类/回归分支解耦 | ~600K | 成熟基线头改进 |

### Batch 2: module-info 频域模块 (6个)

| 模块 | 论文 | 机制 | 复杂度 | RGBT价值 |
|------|------|------|--------|----------|
| FDConv | CVPR2025 | FFT频带分解+独立调制 | 轻量 | IR/RGB频率特征差异化处理 |
| vHeat | CVPR2025 | DCT域热传导exp衰减 | O(N^1.5) | 频率天然分离，全局建模 |
| TOST | ICLR2025 | 二阶矩统计量线性注意力 | O(n) | 双模态分布聚合比较 |
| FADC | CVPR2024 | 频率自适应膨胀率选择 | 轻量 | IR热边缘保留细节 |
| EBlock/DBlock | CVPR2025 DarkIR | FFT幅度调制+空间去模糊 | 轻量 | IR低对比度增强 |
| SFSConv | CVPR2024 | 空间/频率分流+CSU融合 | 82%更少参数 | RGB纹理+IR热信号分工 |

### Batch 3: Mamba/SSM模块 (2个)

| 模块 | 论文 | 机制 | 依赖 |
|------|------|------|------|
| EfficientViM_Block | CVPR2025 | 单头隐藏状态混合SSM | 纯PyTorch |
| WTE_Mamba | CVPR2025 | Haar小波+SSM每频带+多核DWConv | 纯PyTorch |

### Batch 4: 实用工具 (5+2个)

| 工具/损失 | 来源 | 用途 |
|-----------|------|------|
| **NWD** | Normalized Wasserstein Distance | 小目标IoU替代，对微小目标位置偏移更鲁棒 |
| **DIoU NMS / Soft NMS** | DIoU-NMS / Soft-NMS | 更合理的去重策略，DIoU考虑中心点距离，SoftNMS保留高遮挡目标 |
| **ADI / FocusedLoss** | ADI + FocalLoss | 自适应阈值过滤+类别平衡损失，适合双模态难例挖掘 |
| **Siou_CCIoU / InnerIoU / SlideLoss** | SIoU/CIoU/InnerIoU + SlideLoss | IoU变体组合，SlideLoss动态筛选高质量正样本 |
| **Adaptive Training Sample Selection** | ATSS | 自动阈值正负样本分配 |

| 脚本工具 | 来源 | 用途 |
|----------|------|------|
| **compute_flops.py** | objectdetection-tricks | 基于 fvcore 的 FLOPs 精准计算，替代 thop 估算 (已验证32.9%偏差) |
| **compute_parameters.py** | objectdetection-tricks | 逐层参数量/内存/结构可视化 |

### 关键创新点总结

- **纯PyTorch实现SSM** (无mamba-ssm CUDA依赖)
- **频域模块首次系统集成** (FFT/DCT/Gabor)
- 所有模块纯追加，不修改ultralytics核心源码
- 19个YAML配置按7类分目录，层号标注清晰
- NWD/SoftNMS需手动挂载训练流程

### 插入策略总结

| 模块类型 | 典型插入位置 | 示例 |
|----------|-------------|------|
| Conv替代 | backbone/neck Conv层 | Conv_Coord, PConv, FDConv, FADC, SFSConv |
| C3k2替代 | backbone P3/P4/P5 | C3_RFEM, StarBlock, Faster_Block |
| SPPF替代 | P5融合后 | vHeat, TOST, EfficientViM_Block, WTE_Mamba |
| 检测头替代 | 最后一层 | DecoupledHead |
| 上采样替代 | FPN neck | CARAFE |
| 插入增强 | 融合后/IR分支 | EVCBlock, ContextAgg, EBlock, ASF_fusion |

---

## 2026-06-04

### 从 vHeat-main (CVPR 2025) 提取 2 个新模块

基于原论文 `vHeat: Building Vision Models upon Heat Conduction` 的完整源码，提取并实现了两个模块：

#### 1. vHeat_Block（完整热传导块）

| 属性 | 内容 |
|------|------|
| **模块名** | `vHeat_Block` |
| **来源** | vHeat (CVPR 2025) — `classification/models/vHeat.py` 中的 `HeatBlock` |
| **类型** | 频域 Transformer-style block (替换 SPPF/C3k2/C2PSA) |
| **参数量** | +3.55M (P5 位置, scale=s, 含 dwconv+门控+MLP) |
| **核心机制** | Pre-Norm → HCO(DCT→自适应k预测→热扩散衰减→IDCT→SiLU门控→残差) → DropPath → Norm → MLP(1×1Conv扩张4×→GELU→压缩) → DropPath |
| **vs 已有 vHeat** | 我们的 vHeat 仅含 HCO 核心算子；vHeat_Block 是原论文完整结构，增加 dwconv 预处理、SiLU 门控、MLP/FFN、DropPath 正则化 |

**对 RGBT 的价值**:
- dwconv 预处理捕获局部邻域上下文（对红外噪声预平滑）
- SiLU 门控机制选择性传递频率信息
- MLP/FFN 提供通道混合增强跨模态信息交互
- DropPath 正则化对抗红外模态过拟合
- O(N^1.5) 复杂度，低于 Self-Attention 的 O(N²)

**插入策略**: 替换 SPPF（层24），P5 融合特征进入颈部前做全局频域建模

**验证结果**: 导入通过 / forward pass通过 / YAML模型构建通过 / 训练2 epochs完成

#### 2. vHeat_Fusion（跨模态热传导融合）⭐ 论文级创新

| 属性 | 内容 |
|------|------|
| **模块名** | `vHeat_Fusion` |
| **来源** | vHeat (CVPR 2025) 热传导原理的 RGB-T 扩展创新 |
| **类型** | 频域跨模态融合 (替换 Concat) |
| **参数量** | +1.05M (每个融合点, c2=512) |
| **核心机制** | RGB/IR分别→DCT→模态特定k预测(k_rgb小保留细节/k_ir大抑制噪声)→热扩散衰减→跨模态频率门控→频域加权融合→IDCT→空域融合特征 |

**对 RGBT 的价值**:
- **频域天然解耦**: RGB 高频纹理 vs IR 低频热辐射在 DCT 域自然分离
- **自适应去噪**: IR 噪声在频域表现为高频伪影→大 k_ir 自动衰减
- **细节保留**: RGB 纹理在高频→小 k_rgb 保留
- **跨模态频率门控**: 两模态互相指导频率分量选择（而非简单 Concat）
- **首次将热传导原理从单模态 backbone 扩展到跨模态融合**

**插入策略**: 替换 P3/P4/P5 三个尺度的 Concat 融合层（层21/22/23）

**验证结果**: 导入通过 / forward pass通过 / YAML模型构建通过

### 关键修复

- **EMA 兼容性**: 将 `register_buffer` 改为普通 dict 缓存 DCT 矩阵，避免动态 buffer 与 EMA 状态字典不匹配
- **设备兼容性**: 增加设备感知的缓存失效机制，解决模型加载后 CPU→CUDA 迁移问题

---

## 2026-06-20

### 新增 FCMMFusion (跨分支交叉门控融合)

> 来源: FCM-main 中 FCM 的双分支 Cross-Gating 机制改造

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **FCMMFusion** | FCM-main (改造) | vis空间门控(来自ir) + ir通道门控(来自vis) 互补融合 | +0.35M / 融合点 |
| **FCMBlockFusion** | FCM-main (改造) | FCMMFusion + DWConv轻量refine | +0.37M / 融合点 |

**核心创新**:
- 不是简单 Concat / add，而是跨模态门控: 用 IR 的全局统计生成空间门控掩码作用于 vis，用 vis 的全局统计生成通道门控掩码作用于 ir
- 与 CIFusion 的区别: CIFusion 做通道注意力后交叉交换，FCMMFusion 做空间+通道双路交叉门控
- 与 vHeat_Fusion 的区别: vHeat_Fusion 在频域融合，FCMMFusion 在空域融合
- 全部使用 GroupNorm 替代 BatchNorm，兼容 P5 (空间1x1) + batch_size=1 的模型初始化场景

**插入策略**: 替换 P3/P4/P5 三个尺度的 Concat 融合层（层21/22/23）

**验证结果**: 导入通过 / forward pass通过 / YAML模型构建通过 (491 layers, 19.96M params, 47.63 GFLOPs)

---

## 2026-06-21

### EI2Det 创新点改造: 4个跨模态融合模块

> 来源: EI2Det-master (Emerging Image Dual-modal Detection)

| 模块 | 灵感来源 | 核心机制 | 参数量 |
|------|----------|----------|--------|
| **DMAF** | EI2Det DMAF | 差分图GAP+tanh交叉加权, 关注模态间差异 | ~0M额外参数 |
| **QualityWeightedFusion** | EI2Det IWM | GAP+MLP预测天气/质量权重, 自适应加权vis/ir | ~0.4M |
| **EdgeBlendFusion** | EI2Det EFM | Sobel边缘检测+可学习alpha混合, 保留边缘细节 | ~0.8M |
| **EI2Fusion** | EI2Det TransformerFusionBlock | 质量加权+边缘混合全流程融合 | ~1.2M |

**创新点**:
- DMAF: 首次在RGBT融合中使用差分图交叉加权, 天然抑制共同背景
- QualityWeightedFusion: 将EI2Det的天气感知思想改造为特征级质量预测, 不依赖原始图像
- EdgeBlendFusion: 将EI2Det的Sobel边缘混合改造为可学习alpha, 更灵活
- EI2Fusion: 组合质量加权+边缘混合, 模拟EI2Det完整流水线但保持即插即用

**插入策略**: 替换P3/P4/P5三个尺度的Concat融合层(层21/22/23)

**验证结果**: 全部通过(单元测试+YAML构建)
## 2026-06-21

### 新增 11 个模块 (来自 yoloair-main 仓库)

> 来源: yoloair-main (positive666/yolov5_research + iscyy/yoloair)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| CrissCrossAttention | CCNet (arXiv:1811.08838) | 十字交叉注意力, 行+列方向高效non-local近似 | ~82K |
| SOCA | SAN (CVPR) | 二阶通道注意力, 协方差池化+矩阵平方根 | ~66K |
| SKAttention | SKNet (CVPR 2019) | 选择性核注意力, 多尺度卷积核自适应选择 | ~50M(默认) |
| NAMAttention | NAM (arXiv:2111.12419) | BN统计通道注意力, 零额外学习参数 | ~512 |
| S2Attention | S2-MLP (arXiv:2108.01072) | 空间移位注意力, 零参数空间建模 | ~2.1M |
| ACmix | ACmix (arXiv:2203.14957) | 混合注意力+卷积, 可学习rate1/rate2加权 | ~1M |
| BoT3 | BoT (arXiv:2104.14294) | 瓶颈Transformer, MHSA+相对位置编码 | ~2M |
| GSConv | SlimNeck (arXiv:2307.11924) | Ghost Shuffle轻量卷积, DSConv+通道Shuffle | ~100K |
| VoVGSCSP | SlimNeck | CSP+GSBottleneck, 轻量C3k2替代 | ~300K |
| Involution | Involution (CVPR 2021) | 逆卷积算子, 位置感知动态卷积核 | ~500K |
| ASFFFusion | ASFF (arXiv:1911.09516) 改造 | 自适应空间特征融合(双模态版), softmax加权 | ~200K |
| SPPCSPC | CSPNet + SPP | CSP增强SPP, 多尺度MaxPool(5,9,13) | ~2M |
| SPPFCSPC | CSPNet + SPPF | CSP增强快速SPP, 3次MaxPool(5) | ~1.5M |

**核心创新**:
- CrissCrossAttention: O(H·W·(H+W))复杂度的高效non-local, 可建模RGB-T跨模态长距离空间依赖
- SOCA: 二阶统计量(协方差矩阵)捕获RGB-T通道间高阶相关性, 比SE/CBAM更丰富
- ASFFFusion: 将ASFF从单模态多尺度融合改造为双模态融合, 学习RGB和IR的自适应融合权重

**插入策略**: 注意力模块替换C2PSA(层25); 融合模块替换Concat(层21/22/23); 卷积模块替换C3k2(层4); 上下文模块替换SPPF(层24)

**验证结果**: 全部通过(导入+forward+YAML构建)

---

## 2026-06-23

### 新增 LCAFNet 跨模态交叉注意力融合模块 (2个)

> 来源: LCAFNet (RGB-T dual-stream detection framework)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| Att_HAFFormer | LCAFNet HAFFormer | 层级注意力融合: 双向CrossAttention_S(DW-Conv QKV) + sigmoid门控融合 | ~8M (c2=512) |
| Att_CrossAttention_M | LCAFNet CrossAttention_M | 双向DW-Conv交叉注意力: 共享QKV投影, rgb_q@ir_k + ir_q@rgb_k | ~4M (c2=512) |

**核心创新**:
- HAFFormer: 将单向交叉注意力扩展为双向, 加上门控融合机制, 让两模态互相增强后自适应融合
- CrossAttention_M: 使用深度可分离卷积做QKV投影(而非Transformer的Linear), 保留空间局部性, 参数更少
- 两者均使用 einops.rearrange 实现多头注意力的 reshape, 与现有 ICAFusion(Linear QKV) 形成互补

**插入策略**: 替换 Concat (层21/22/23), 原位替换, 无层号位移

**验证结果**: 导入+forward+YAML构建 全部通过
- HAFFormer: 512 layers, 32.7M params, 90.3 GFLOPs
- CrossAttention_M: 470 layers, 23.2M params, 75.2 GFLOPs

### 新增 GatherExcite 注意力模块 (来自 timm/Fracture_Detection)

> 来源: Gather-Excite Networks (NeurIPS 2018), timm 库, Fracture_Detection_Improved_YOLOv8

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| Att_GatherExcite | timm GatherExcite | 局部/全局 AvgPool 聚合 + MLP 通道门控, extent 可选 | ~32K (c=1024, r=16) |

**核心创新**:
- 与 SE 的区别: SE 只用全局 GAP, GatherExcite 可用 extent>0 做局部聚合, 保留空间上下文
- extent=0 退化为 SE 变体, extent=2/4/8 用局部 AvgPool 保留空间分辨率
- 极轻量: MLP 降维比 16:1, 参数量与 SE 相当

**插入策略**: 替换 C2PSA (层25), 原位替换, 无层号位移

**验证结果**: 导入+forward+YAML构建 全部通过
- GatherExcite: 430 layers, 14.3M params, 33.6 GFLOPs

### 新增 BinaryAttention 1-bit QK注意力模块 (CVPR2026)

> 来源: CVPR2026 - BinaryAttention: One-Bit QK-Attention for Vision and Diffusion Transformers
> 论文: https://arxiv.org/abs/2509.25164 (The Hong Kong Polytechnic University & OPPO Research Institute)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| BinaryAttention | CVPR2026 | 1-bit二值化QK + 位运算注意力 + 8-bit量化PV | ~2M (c=512) |
| BinaryChannelAttention | CVPR2026 | 轻量级二值通道注意力(适配CNN) | ~4K (c=512) |
| C3k2_BinaryAttention | CVPR2026 | C3k2 + BinaryBottleneck集成 | ~5M (c=1024) |

**核心创新**:
- **1-bit量化**: 将query和key二值化为{-1, 1}，用XNOR+popcount位运算替代浮点点积，计算成本降至1/32
- **理论保证**: 定理1证明二值注意力保留原始协方差结构(协方差矩阵与Gram矩阵等价非零特征谱)
- **可学习偏置**: 引入偏置项减轻1-bit量化导致的幅度信息丢失和注意力分布偏移
- **混合量化**: 注意力系数用无符号8-bit，value用对称8-bit，实现端到端加速
- **性能**: A100 GPU上比FlashAttention2快2倍以上，精度匹配或超越全精度基线

**插入策略**: 替换backbone的P4和P5层C3k2为C3k2_BinaryAttention，原位替换无层号位移
- RGB分支: 层8(C3k2→C3k2_BinaryAttention), 层10(C3k2→C3k2_BinaryAttention)
- IR分支: 层18(C3k2→C3k2_BinaryAttention), 层20(C3k2→C3k2_BinaryAttention)

**验证结果**: 导入+forward+YAML构建 全部通过
- BinaryAttention: 442 layers, 20,185,945 params, 38.21 GFLOPs

### 新增 AFFN 频域自相关融合模块

> 来源: 用户提供 (频域自相关融合网络)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| AFFN | 用户提供 | FFT频域变换 + 自相关功率谱 + 频域/空间域双重增强 | ~1M (c=512) |
| C2PSA_AFFN | 用户提供 | C2PSA + AFFN集成, 替换PSABlock | ~3M (c=1024) |

**核心创新**:
- **自相关功率谱**: R(k) = Xf(k) * conj(Xf(k))，捕获频域全局上下文
- **频域增强**: Xf_new = Xf + α * power，可学习权重α自适应增强频域特征
- **空间域增强**: x_out = x_out + β * R，自相关逆变换增强空间域特征
- **双重融合**: 频域和空间域双重增强，互补提升特征表达能力
- **GELU门控**: DWConv + GELU门控机制，增强非线性表达

**插入策略**: 替换C2PSA (层25) 为 C2PSA_AFFN，原位替换无层号位移

**验证结果**: 导入+forward+YAML构建+训练冒烟 全部通过
- AFFN: 433 layers, 14,972,251 params, 34.20 GFLOPs
- 训练测试: 2 epochs 运行正常，loss收敛

### 新增 Flickerformer模块集合 (PAM, FSAS, SCAM, PhaseGuidedFilter)

> 来源: Flickerformer (视频去闪烁网络)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| PAM | Flickerformer | 相位注意力: FFT提取相位→MLP处理→重建频域 | ~15M |
| FSAS | Flickerformer | 频域-空间自注意力: QK频域交互×V空间调制 | ~15M |
| SCAM | Flickerformer | 空间-通道注意力: 三路分割+双向交叉注意力+DFFN | ~26M |
| PhaseGuidedFilter | Flickerformer | 相位引导滤波: 频域相位相关性引导特征滤波 | ~159M |
| C2PSA_PAM | Flickerformer | C2PSA + PAM集成 | ~15M |
| C2PSA_FSAS | Flickerformer | C2PSA + FSAS集成 | ~15M |
| C2PSA_SCAM | Flickerformer | C2PSA + SCAM集成 | ~26M |

**核心创新**:
- **PAM**: 只处理相位信息，保留幅度不变。理论：相位包含图像结构信息，幅度包含能量信息
- **FSAS**: QK在频域计算，V在空间域调制。频域点积替代空间点积，O(N)复杂度
- **SCAM**: 三路分割+双向交叉注意力（x1→x2, x3→x2），信息流更丰富
- **PhaseGuidedFilter**: 利用频域相位相关性进行跨特征引导，特别适合RGBT融合

**插入策略**:
- PAM/FSAS/SCAM: 替换C2PSA (层25)，原位替换无层号位移
- PhaseGuidedFilter: 替换Concat (层21/22/23)，原位替换无层号位移

**验证结果**: 导入+forward+YAML构建 全部通过
- PAM: 435 layers, 15,033,945 params, 34.24 GFLOPs
- FSAS: 435 layers, 15,040,601 params, 34.25 GFLOPs
- SCAM: 517 layers, 25,881,721 params, 42.92 GFLOPs
- PhaseGuidedFilter: 482 layers, 158,703,036 params, 417.72 GFLOPs (参数量较大)

### 新增 PolyNeXt模块集合 (PolyAttention, PolyConv)

> 来源: PolyNeXt (ICLR2024 多项式神经网络)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|---------|--------|
| PolyAttention | PolyNeXt ICLR2024 | 多项式注意力: ((q@k.T)*scale+1)^4 替代softmax | ~16M |
| PolyConv | PolyNeXt | 多项式卷积: conv1(x)*conv2(x).flip(dims=[1]) | ~17M |
| PolyMLP | PolyNeXt | 多项式MLP: high*low门控交互 | - |
| C2PSA_PolyAttention | PolyNeXt | C2PSA + PolyAttention集成 | ~16M |
| C3k2_PolyConv | PolyNeXt | C3k2 + PolyConv集成 | ~17M |

**核心创新**:
- **PolyAttention**: `((q @ k.T) * scale + 1) ** 4` 替代softmax
  - 理论：多项式可以近似任意连续函数（Stone-Weierstrass定理）
  - 优势：无需exp操作，计算更友好，更适合硬件加速
  - 可学习缩放参数：sigmoid(scale)控制多项式强度
- **PolyConv**: `conv1(x) * conv2(x).flip(dims=[1])`
  - 创新：通道翻转(dims=[1])实现跨通道交互
  - 元素级乘法实现高阶特征交互
  - 增强特征表达能力
- **PolyMLP**: `high * low` 门控交互
  - 分割通道为两半，元素级乘法实现门控

**插入策略**:
- PolyAttention: 替换C2PSA (层25)，原位替换无层号位移
- PolyConv: 替换C3k2 (层8/10/18/20)，原位替换无层号位移

**验证结果**: 导入+forward+YAML构建 全部通过
- PolyAttention: 493 layers, 16,483,225 params, 35.40 GFLOPs
- PolyConv: 406 layers, 17,001,369 params, 35.31 GFLOPs

---

## 2026-06-30

### 新增 WDAM 小波双注意力模块

> 来源: wechat.md 中的 WDAM 模块

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **WDAM** | wechat.md | Haar DWT + 窗口注意力 + 高频卷积 | ~3M |
| **C2PSA_WDAM** | 自实现 | Split-Concat 包装器 | ~3M |

**核心创新**:
1. **小波域差异化处理**: Haar DWT 将特征分解为 LL/LH/HL/HH 四个频带，低频用窗口注意力，高频用卷积
2. **窗口注意力 + Shift**: 窗口内计算注意力降低复杂度，Shift 操作增强跨窗口信息交流
3. **相对位置偏置**: 可学习的位置偏置表，增强空间感知能力
4. **高频调制 Value**: 用 LH、HL 高频信息调制注意力的 Value，保留边缘/纹理信息

**与已有频域模块的区别**:
| 模块 | 频域变换 | 注意力机制 | 特点 |
|------|----------|------------|------|
| FDConv | FFT | 频带分解+复数权重 | 全局频域解耦 |
| vHeat | DCT | 热传导扩散 | 全局建模 O(N^1.5) |
| PAM | FFT | 相位注意力 | 相位增强 |
| **WDAM** | **DWT(小波)** | **窗口注意力** | **多尺度频域+局部注意力** |

**插入策略**:
- C2PSA_WDAM: 替换 C2PSA (层25)，原位替换无层号位移

**验证结果**: 导入+forward+YAML构建 全部通过
- WDAM: 444 layers, 18,120,937 params, 36.71 GFLOPs

## 2026-06-30

### 新增 SPDConv + CSPOmniKernel 模块 (来自 GeoFuse-YOLO)

> 来源: GeoFuse-YOLO (SOEP: Small Object Enhance Pyramid)
> 仓库: GeoFuse-YOLO (RGB-NIR-Geo 多模态核桃检测)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **SPDConv** | GeoFuse-YOLO/SOEP | 空间到深度转换(4x通道)+3x3卷积+上采样+残差 | ~0.1M |
| **CSPOmniKernel** | GeoFuse-YOLO/SOEP | CSP+多尺度方向分离DWConv+通道注意力+FGM门控 | ~0.05M |

**核心创新**:

SPDConv:
1. **空间到深度转换**: 将H×W特征分解为4个(H//2)×(W//2)子特征, 通道变为4C, 保留更丰富空间位置信息
2. **分辨率保持**: 通过上采样恢复原分辨率, 与残差连接相加, 适合替换C3k2
3. **小目标增强**: 对RGBT中远距离行人/车辆等小目标, IR模态特征稀疏的问题有针对性改善

CSPOmniKernel:
1. **多尺度全方向卷积**: 水平(1×k)和垂直(k×1)分离的深度卷积, 捕获不同方向不同尺度特征
2. **CSP轻量结构**: 激进降维(reduction=2/4/8)+极小扩展比(e=0.0625), 参数极少
3. **FGM频域门控**: 全局池化门控×局部深度卷积, 模拟高低频分离, 低维通道启用
4. **BatchNorm-free**: 全程无BatchNorm, P5层1×1空间安全

**与已有conv模块的区别**:
| 模块 | 机制 | 特点 |
|------|------|------|
| SPDConv | 空间到深度+残差 | 保持分辨率, 小目标增强 |
| CSPOmniKernel | CSP+全方向DWConv+FGM | 极轻量, 多尺度方向感知 |
| GSConv | GS垂直+水平+残差 | 轻量conv, 无多尺度 |
| LSKA | 大核可分离注意力 | 空间注意力, 非conv替换 |

**插入策略**:
- SPDConv: 替换层4/14 (RGB/IR P2), 小目标增强最佳位置
- CSPOmniKernel: 替换层6/16 (RGB/IR P3), 多尺度特征提取
- 原位替换, n=2→n=1 (两模块均不在repeat_modules中)

**验证结果**: 待验证 (用户跳过验证步骤)

---

## 2026-07-03

### 新增 C2Former 跨模态可变形交叉注意力融合模块

> 来源: C2Former (旋转目标检测中的双模态融合模块)
> 论文: C2Former: Cross-modal Cross-former for RGB-T Object Detection

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **C2Former_Fusion** | C2Former | 双向可变形交叉注意力 | ~2M |
| **C2PSA_C2Former** | 自实现 | Split-Concat + 交叉注意力 | ~2M |
| **ModalityNorm** | C2Former | 模态归一化 | ~0.1M |

**核心创新**:
1. **可变形交叉注意力**: 使用 offset 机制实现可变形采样，自适应对齐两个模态的特征位置
2. **双向交叉注意力**: VIS 查询 IR 特征，IR 查询 VIS 特征，实现真正的跨模态交互
3. **ModalityNorm**: 用源模态统计量（均值、标准差）重缩放参考模态，处理 VIS 和 IR 模态间的分布差异
4. **残差交叉连接**: vis_x += out_lwir, lwir_x += out_vis，增强跨模态信息流动

**与已有融合模块的区别**:
| 模块 | 机制 | 特点 |
|------|------|------|
| CIFusion/AFF/iAFF | 简单加权融合 | 轻量但交互有限 |
| ICAFusion | 标准交叉注意力 | 无位置自适应 |
| **C2Former** | **可变形交叉注意力+ModalityNorm** | **自适应跨模态对齐** |

**插入策略**:
- C2Former_Fusion: 替换层21/22/23 Concat (P3/P4/P5 融合)，原位替换无层号位移
- C2PSA_C2Former: 替换层25 C2PSA，原位替换无层号位移

**验证结果**: 导入+forward 全部通过
- C2Former_Fusion: 双输入融合模块
- C2PSA_C2Former: 单输入 C2PSA 包装器

---

## 2026-07-03 (续)

### 新增 MARSS 模块集合 (REM, RADE, RFAF, RADM)

> **论文**: [MARSS: Radar Semantic Segmentation via Modular Attention and State Space Models](https://openaccess.thecvf.com/content/CVPR2026/papers/Chen_MARSS_Radar_Semantic_Segmentation_via_Modular_Attention_and_State_Space_CVPR_2026_paper.pdf)
> **来源**: CVPR 2026

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **REM** | MARSS | 轴向卷积模拟小波四子带分解 | ~0.1M |
| **RADE** | MARSS | 通道+空间注意力级联去噪 | ~0.2M |
| **RFAF_Fusion** | MARSS | 十字形感受野+多尺度空洞卷积 | ~0.5M |
| **RADM** | MARSS | 轴向自注意力+DWConv三分支 | ~0.3M |
| **C2PSA_RADM** | 自实现 | Split-Concat + RADM | ~0.3M |

**核心创新**:
1. **REM (小波增强模块)**: 用轴向卷积(1×3, 3×1)模拟 DWT 的四子带分解
   - LL子带: 1×1卷积 (低频全局信息)
   - LH子带: 1×3 → 3×1 → 3×3 (水平高频)
   - HL子带: 3×1 → 1×3 → 3×3 (垂直高频)
   - HH子带: 3×3 → 3×3 (对角高频)

2. **RADE (去噪编码器)**: CBAM式级联注意力
   - 通道注意力: GAP → MLP → Sigmoid (抑制噪声通道)
   - 空间注意力: AvgPool+MaxPool → 7×7 Conv → Sigmoid (增强目标区域)

3. **RFAF (自适应融合)**: 多尺度融合 + 区域级注意力
   - Stage I: 通道+空间注意力学习模态重要性
   - Stage II: 十字形感受野分别处理水平和垂直邻域
   - 多尺度空洞卷积分支捕获不同尺度上下文

4. **RADM (状态空间解码)**: 三分支并行
   - H-axis分支: 沿高度轴做轴向自注意力
   - W-axis分支: 沿宽度轴做轴向自注意力
   - Global分支: Depth-wise Conv + Self-Attention

**与已有模块的区别**:
| 模块 | 机制 | 特点 |
|------|------|------|
| CBAM | 标准通道+空间注意力 | 无去噪设计 |
| WDAM | 小波域窗口注意力 | 无轴向卷积 |
| C2Former | 可变形交叉注意力 | 无区域级注意力 |
| **MARSS** | **轴向卷积+区域注意力+轴向自注意力** | **专为雷达/红外图像设计** |

**插入策略**:
- RFAF_Fusion: 替换层21/22/23 Concat (P3/P4/P5 融合)，原位替换无层号位移
- C2PSA_RADM: 替换层25 C2PSA，原位替换无层号位移

**验证结果**: 导入+forward 全部通过
- REM: 小波增强模块
- RADE: 去噪编码器
- RFAF_Fusion: 双输入融合模块
- RADM: 状态空间解码模块
- C2PSA_RADM: 单输入 C2PSA 包装器

---

## 2026-07-05

### 新增 CKConv 多尺度十字形卷积模块

> **论文**: [IEEE](https://ieeexplore.ieee.org/document/11320952)
> **来源**: wechat.md

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **CKConv** | IEEE | 多尺度十字形卷积核 | ~0.3M |
| **C3k2_CKConv** | 自实现 | C3k2 + CKConv | ~0.5M |

**核心创新**:
1. **多尺度十字形卷积核**: 支持 [3, 5, 7, 9] 多种核大小，每个核大小使用轴向卷积实现十字形感受野
2. **残差连接**: body 分支(3×3 DWConv) + head 分支(轴向卷积)，保留局部特征和方向性特征
3. **多尺度融合**: 所有尺度分支拼接后 1×1 融合，自适应选择不同尺度的特征

**与已有卷积模块的区别**:
| 模块 | 机制 | 特点 |
|------|------|------|
| 标准 Conv | 方形卷积核 | 计算量大，无方向性 |
| DWConv | 深度可分离卷积 | 轻量但单一尺度 |
| REM | 轴向卷积模拟小波 | 单一尺度，四子带分解 |
| **CKConv** | **多尺度十字形卷积核** | **多尺度并行+轴向卷积** |

**插入策略**:
- C3k2_CKConv: 替换层4/6/8/10/14/16/18/20 C3k2，原位替换无层号位移

**验证结果**: 导入+forward 全部通过
- CKConv: 多尺度十字形卷积核
- C3k2_CKConv: 集成CKConv的C3k2

---

## 2026-07-05 (续)

### 新增 CROWn 微观多相共注意力模块

> **论文**: CROWn (Coset-fibRated micrO-local co-attention Network)
> **来源**: CROWn.py (3D 医学图像分割网络)

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **μPCAD_2D** | CROWn | 小波变换+跨源注意力 | ~0.5M |
| **C2PSA_μPCAD** | 自实现 | Split-Concat + μPCAD | ~0.5M |
| **CrossSourceMHA** | CROWn | 跨源多头注意力 | ~0.2M |

**核心创新**:
1. **μPCAD (微观多相共注意力降采样器)**:
   - Max/Avg 池化 + 小波分解 → 多尺度特征
   - 跨源注意力: Q=Max, K=Avg, V=Wavelet
   - 局部融合: 6路特征拼接 → 3×3 Conv + MLP
   - SE 通道注意力自适应加权

2. **CrossSourceMHA (跨源多头注意力)**:
   - 支持不同来源的 Q, K, V
   - 空间降维 (SR) 用于 K/V，减少计算量
   - GroupNorm 归一化

3. **DWT2D_Haar (Haar 小波分解)**:
   - 使用卷积实现的 Haar 小波分解
   - 输出 LL/LH/HL/HH 四个子带
   - 捕获多分辨率特征

**与已有模块的区别**:
| 模块 | 机制 | 特点 |
|------|------|------|
| WDAM | 小波域窗口注意力 | 单一输入，无跨源 |
| C2Former | 可变形交叉注意力 | 无小波变换 |
| **CROWn** | **小波变换+跨源注意力** | **多源Q/K/V+小波分解** |

**插入策略**:
- C2PSA_μPCAD: 替换层25 C2PSA，原位替换无层号位移

**验证结果**: 导入+forward 全部通过
- μPCAD_2D: 微观多相共注意力降采样器 (有下采样)
- C2PSA_μPCAD: 微观多相共注意力 C2PSA 包装器 (无下采样)
- CrossSourceMHA: 跨源多头注意力

---

## 2026-07-05 (续)

### 新增 RT-SFOD 训练策略损失函数

> **论文**: RT-SFOD (ECCV 2026)
> **来源**: RT-SFOD/scripts/YOLO26/stage2_rtsfod_yolo26.py

| 模块 | 来源 | 核心机制 | 参数量 |
|------|------|----------|--------|
| **variance_loss** | RT-SFOD | 方差损失，鼓励特征多样性 | 0 |
| **covariance_loss** | RT-SFOD | 协方差损失，减少特征冗余 | 0 |
| **MARD_Loss** | RT-SFOD | 多尺度特征多样性正则化 | 0 |
| **DualHeadFusion** | RT-SFOD | 双头伪标签融合 | 0 |

**核心创新**:
1. **Variance Loss (方差损失)**:
   - 计算特征的标准差，如果低于阈值 gamma 则惩罚
   - 鼓励不同模态的特征保持多样性，避免特征坍缩
   - 公式: `relu(gamma - std).mean()`

2. **Covariance Loss (协方差损失)**:
   - 计算特征的协方差矩阵，惩罚非对角线元素
   - 减少特征冗余，提升特征效率
   - 公式: `off_diag.pow(2).sum() / (c * (c - 1))`

3. **MARD_Loss (多尺度自适应表示多样化)**:
   - 在 P3/P4/P5 多尺度特征上应用方差+协方差损失
   - 提升特征的区分能力，有利于检测

4. **DualHeadFusion (双头伪标签融合)**:
   - 利用 YOLO 的 one2one 和 one2many 双头输出
   - 通过 IoU 去重融合，提升伪标签质量
   - 适合半监督双模态学习

**与已有损失函数的区别**:
| 损失函数 | 机制 | 特点 |
|----------|------|------|
| 标准 YOLO Loss | CIoU + BCE | 基础检测损失 |
| Cross Modal Consistency | 余弦相似度 | 跨模态一致性 |
| **RT-SFOD Loss** | **方差+协方差正则化** | **特征多样性** |

**使用方式**:
```python
from ultralytics.nn.ExtraModules.loss.RT_SFOD_Loss import MARD_Loss, DualHeadFusion

# MARD 损失 (辅助损失)
mard_loss_fn = MARD_Loss(alpha=1.0, beta=0.1, gamma=1.0)
mard_result = mard_loss_fn(feats=[p3, p4, p5])
total_loss = det_loss + 0.05 * mard_result['mard_loss']

# 双头融合 (推理时)
dhf = DualHeadFusion(tau_o2o=0.5, tau_o2m=0.5, tau_no=0.2, tau_dup=0.7)
fused_boxes = dhf(boxes_one2one, boxes_one2many)
```

---

## 2026-07-13

### 新增 10 个模块（HVPNet / rmae-progress / UCMNet）

> 来源: `exps/HVPNet-main`, `exps/rmae-progress-main`, `exps/UCMNet-main`, `exps/SFDNet-HBB`
> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-07-13/`

#### SRA: Strip Recurrent Attention

- **来源**: HVPNet (RGB-T 显著目标检测)
- **核心机制**: 对 H/W 方向分别做条带池化 → 分组深度可分离卷积+门控(核3,5,7,9) → 下采样后 QKV 自注意力 → 通道门控
- **参数量**: 替换 Conv 后 14.9M / 429.9 GFLOPs
- **改进动机**: 条带注意力在 H/W 方向上保持全局感知, 适合捕捉 RGB-T 水平/垂直方向的模态差异

#### SCA: Spatial-Channel RGB-T Fusion

- **来源**: HVPNet
- **核心机制**: RGB×IR 乘积路径(捕捉共现) + RGB+IR 求和路径(保留独立) → SRA 空间+通道注意力
- **参数量**: 替换 Concat 后 28.9M / 58.3 GFLOPs
- **改进动机**: 同时建模模态间共现和独立特征, 比简单 Concat 更有效地融合双模态

#### GFM: Global Fusion Module

- **来源**: HVPNet
- **核心机制**: Concat(RGB, IR) → DWPWConv 降维 → SAttention(简化的QKV自注意力+局部DWConv) → 残差+GELU
- **参数量**: 替换 Concat 后 24.8M / 51.0 GFLOPs (最轻量fusion)
- **改进动机**: 全局自注意力建模跨模态长程依赖, DWPWConv 保持轻量

#### EDS: Edge-aware Dynamic Sampling Fusion

- **来源**: HVPNet
- **核心机制**: RGB×IR + RGB+IR 双路径融合 → 4分支空洞卷积(dilation=1,3,5,7) → 拼接融合
- **参数量**: 替换 Concat 后 66.1M / 126.3 GFLOPs
- **改进动机**: 小空洞捕获 RGB 纹理边缘, 大空洞捕获 Thermal 热辐射轮廓

#### HA: Holistic Attention

- **来源**: HVPNet
- **核心机制**: 高斯核对 attention map 平滑(模拟视觉皮层中心-环绕抑制) → min-max归一化 → 加权特征图
- **参数量**: 15.2M / 34.4 GFLOPs
- **改进动机**: 高斯平滑消除注意力图中的孤立点噪声

#### ProgressiveAgg: 渐进式特征聚合

- **来源**: rmae-progress ProGRess + HVPNet 密集融合
- **核心机制**: PLF(渐进跳跃融合, 从深到浅, 上采样+拼接+1x1融合) + LCAR(轻量1x1通道注意力+残差) + Bottleneck融合(上采样到最大分辨率后拼接)
- **参数量**: 16.4M / 42.6 GFLOPs
- **改进动机**: 渐进式融合保留多尺度信息, LCAR 增强通道选择性

#### Aggregation: 3尺度密集注意力聚合

- **来源**: HVPNet
- **核心机制**: 3个尺度特征上采样后逐元素乘跨尺度交互 → 密集拼接 + 卷积输出 attention map
- **参数量**: 33.7M / 232.2 GFLOPs
- **改进动机**: 逐元素乘跨尺度交互能有效融合不同分辨率的模态特征

#### RFBBranch: 4分支非对称RFB

- **来源**: HVPNet
- **核心机制**: 4分支分别使用 dilation=3,5,7, 每分支先 1x3/3x1 非对称卷积增强边缘方向感知
- **改进动机**: 与现有的 BasicRFB(3分支,dilation=1,2,3) 和 BasicRFB_small(4分支, dilation=1,3,3,5) 机制不同

#### COI: Conv-One-Identity

- **来源**: HVPNet
- **核心机制**: Shortcut + Depthwise Conv + 1x1 Conv 三条并行路径相加 → GELU
- **参数量**: 15.0M / 1734.7 GFLOPs (替换全部Conv时)
- **改进动机**: 极轻量基础组件, 三重残差连接

#### MemoryAttention: 记忆增强注意力

- **来源**: UCMNet (IG_MSA_memory)
- **核心机制**: 可学习码本{(K,C)} + prompt向量 → 输入通过余弦相似度匹配 memory → soft label加权聚合 → 残差输出
- **参数量**: 15.4M / 33.2 GFLOPs (最轻量)
- **改进动机**: Memory bank 学习RGB-T 模态共享特征原型, 增强模态不变特征响应

## 2026-08-02

### 方案1: COI-DASI-Asymmetric — 模态特异性非对称 backbone 增强

> **来源**: COI (HVPNet) + DASI (红外小目标2024) 组合创新
> **配置**: `ultralytics/cfg/models/26-RGBT/2026-08-02/conv/yolo26-RGBT-midfusion-COI-DASI-Asymmetric.yaml`

**核心创新**:
1. **模态特异性非对称设计**: RGB 分支用 COI（三重残差, 保留纹理细节），IR 分支用 DASI（多空洞率 dilation=2/4/6/8, 捕获红外小目标热特征）
2. **stride 对齐**: DASI 添加 stride 参数（`DASI, [64, 2]`），两分支空间分辨率匹配，Concat 无冲突
3. **差异化增强动机**: RGB 依赖纹理/边缘/颜色，COI 的三重残差（shortcut+DWConv+1x1）适合；IR 依赖热分布/小目标，DASI 的多空洞率大感受野适合

**改进动机**: 现有 RGB-T 检测均采用对称 backbone（RGB 和 IR 共享结构），忽略了两个模态的物理特性差异。RGB 模态信息丰富于纹理/颜色，IR 模态信息集中在热分布和小目标响应，两者最优增强策略不同。

**插入策略**: 原位替换，无层号位移。RGB 替换层 2/3/5（COI），IR 替换层 12（DASI）

**代码改动**: `DASI.py` 添加 `s=1` 参数，proj 使用 `stride=s`

---

### 方案2: DASI-ProgAgg — Backbone+Neck 双层组合增强

> **来源**: DASI (红外小目标2024) + ProgressiveAgg (ProGRess+HVPNet) 组合创新
> **配置**: `ultralytics/cfg/models/26-RGBT/2026-08-02/neck/yolo26-RGBT-midfusion-DASI-ProgAgg.yaml`

**核心创新**:
1. **双层叠加**: Backbone 浅层 DASI 增强小目标特征 + Neck ProgressiveAgg 渐进多尺度聚合
2. **分辨率协同**: DASI 无 stride（提升一级分辨率），ProgressiveAgg 内部 F.interpolate 兼容不同分辨率
3. **互补机制**: DASI 的大空洞率在浅层捕获小目标 → ProgressiveAgg 在 neck 渐进融合 P5→P4→P3，形成"浅层增强→多层聚合"的完整链路

**改进动机**: DASI 单独 mAP50=0.886（全榜第1），ProgressiveAgg 单独 FPS=88.14（全榜最高）。两者作用位置不同（backbone vs neck），机制互补（小目标增强 vs 多尺度聚合），组合后可能同时提升精度和保持合理速度。

**插入策略**: Backbone 原位替换（DASI at 2/12），Neck 插入新层 32（ProgressiveAgg），层号位移 +2

---

### 方案3: COI-Lite — 选择性深度 (仅替换最浅层)

> **来源**: COI (HVPNet) 消融实验
> **配置**: `ultralytics/cfg/models/26-RGBT/2026-08-02/conv/yolo26-RGBT-midfusion-COI-Lite.yaml`

**核心创新**:
1. **选择性深度**: 仅替换最浅层（P1/2, 层 2/12），其余 Conv 保持标准
2. **效率优先**: COI-6层 FLOPs=108.96G, COI-Lite 实测仅 34.84G（+0.43G vs 基线），几乎零成本
3. **消融实验设计**: 对比 COI-6层(108.96G) vs COI-2层(34.84G)，论证浅层增强的边际收益递减

**改进动机**: COI 替换全部 6 个 Conv 时 FLOPs 飙升（108.96G vs 基线 34.41G），FPS 降至 12.84。但浅层（P1/2）对小目标纹理最关键，深层 Conv 的增强收益递减。选择性深度在保持精度的同时大幅降低计算量。

**插入策略**: 原位替换，无层号位移。仅层 2/12 替换为 COI（stride=2），层 3/5/13/15 保持 Conv

**验证结果**: 待训练验证

