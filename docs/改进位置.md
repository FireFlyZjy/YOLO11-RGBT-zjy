# 改进位置

> 创建日期: 2026-05-13
> 相关文档: [指令.md](指令.md) | [创新点.md](创新点.md) | [改进位置.md](改进位置.md) | [论文级创新点.md](论文级创新点.md) | [实验方案.md](实验方案.md) | [实验记录.md](实验记录.md) | [模块清单.md](模块清单.md)
> 基线模板: `ultralytics/cfg/models/26-RGBT/yolo26-RGBT-midfusion.yaml`
> 说明: 每次改进在文档末尾追加。同日改进不加新日期头，不同日期改进先加 `## YYYY-MM-DD` 再追加内容。

---

## 2026-05-13

### 新增11个注意力模块YAML配置文件

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-13/`

基于 `yolo26-RGBT-midfusion.yaml`，在双模态Concat融合之后(P3/P4/P5)各插入一个注意力模块。

**文件修改对照表**:

| 配置文件 | 模块 | 改动 |
|----------|------|------|
| `yolo26-RGBT-midfusion-Att_CBAM.yaml` | CBAM 通道+空间 | 新增3行 + 修改5行 |
| `yolo26-RGBT-midfusion-Att_Coord.yaml` | Coordinate Attention | 同上 |
| `yolo26-RGBT-midfusion-Att_SimAM.yaml` | SimAM 无参数3D | 同上 |
| `yolo26-RGBT-midfusion-Att_CPCA.yaml` | CPCA 通道先验+多尺度 | 同上 |
| `yolo26-RGBT-midfusion-Att_EMA.yaml` | EMA 分组交叉空间 | 同上 |
| `yolo26-RGBT-midfusion-Att_ECA.yaml` | ECA-Net 1D卷积通道 | 同上 |
| `yolo26-RGBT-midfusion-Att_Shuffle.yaml` | ShuffleAttention | 同上 |
| `yolo26-RGBT-midfusion-Att_LSKA.yaml` | LSKA 大核空间 | 同上 |
| `yolo26-RGBT-midfusion-Att_Triplet.yaml` | Triplet Attention | 同上 |
| `yolo26-RGBT-midfusion-Att_GAM.yaml` | GAM 全局注意力 | 同上 |
| `yolo26-RGBT-midfusion-Att_ELA.yaml` | ELA 方向局部 | 同上 |

**Backbone改动** (每个YAML):

```
# 新增3行 (在P3/P4/P5 Concat之后):
- [-1, 1, Att_XXX, [512]]   # P3注意力 (新Layer 22)
- [-1, 1, Att_XXX, [512]]   # P4注意力 (新Layer 24)
- [-1, 1, Att_XXX, [1024]]  # P5注意力 (新Layer 26)
```

**Head改动** (层号全部+3):

| 行 | 原内容 | 新内容 | 原因 |
|----|--------|--------|------|
| 30 | `[[-1, 22], 1, Concat, [1]]` | `[[-1, 24], ...]` | P4引用: Concat(layer22) → Att(layer24) |
| 33 | `[[-1, 21], 1, Concat, [1]]` | `[[-1, 22], ...]` | P3引用: Concat(layer21) → Att(layer22) |
| 36 | `[[-1, 28], 1, Concat, [1]]` | `[[-1, 31], ...]` | head引用 +3 |
| 39 | `[[-1, 25], 1, Concat, [1]]` | `[[-1, 28], ...]` | C2PSA引用 +3 |
| 41 | `[[31, 34, 37], 1, Detect, [nc]]` | `[[34, 37, 40], ...]` | Detect输入 +3 |

**层号位移对照** (Backbone):

| 原Layer | 新Layer | 模块 | 变化 |
|---------|---------|------|------|
| - | 22 | Att_XXX | 新增 |
| - | 24 | Att_XXX | 新增 |
| - | 26 | Att_XXX | 新增 |
| 24 | 27 | SPPF | +3 |
| 25 | 28 | C2PSA | +3 |

**层号位移对照** (Head):

| 原Layer | 新Layer | 模块 |
|---------|---------|------|
| 26 | 29 | nn.Upsample |
| 27 | 30 | Concat |
| 28 | 31 | C3k2 |
| 29 | 32 | nn.Upsample |
| 30 | 33 | Concat |
| 31 | 34 | C3k2 |
| 32 | 35 | Conv |
| 33 | 36 | Concat |
| 34 | 37 | C3k2 |
| 35 | 38 | Conv |
| 36 | 39 | Concat |
| 37 | 40 | C3k2 |
| 38 | 41 | Detect |

### 代码文件修改

| 文件 | 改动 |
|------|------|
| `ultralytics/nn/ExtraModules/common.py` | 末尾追加~400行，11个 `Att_XXX` 类，每段带 `(2026-05-13)` 日期标记 |
| `ultralytics/nn/tasks.py` | 第1043行 `base_modules` 追加11个模块注册，带日期和功能注释 |
| `ultralytics/nn/ExtraModules/__init__.py` | 补充 ACBlock / BlazeBlock / GatingContext 导入（之前遗漏） |
| `val_zjy_full.py` | 新建，带CSV输出的完整验证脚本 |

### 架构示意

```
        Visible Branch              Infrared Branch
             |                            |
          C3k2 (P3)                  C3k2 (P3)
             |                            |
        ┌──────────────────────────────────┐
        │        Concat (Layer 21)          │  ← 双模态拼接
        └──────────────────────────────────┘
                        |
        ┌───────────────────────────────┐
        │   Att_XXX (Layer 22) ← NEW    │  ← 注意力筛选
        └───────────────────────────────┘
                        |
              (相同结构在 P4/P5)
                        |
                      SPPF
                        |
                  Neck → Detect
```

---

## 2026-05-14

### 新增 AIFI Transformer自注意力模块

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-14/`
> 来源: RT-DETR, yolo-improve/yolov5-AIFI.py

基于 `yolo26-RGBT-midfusion.yaml`，将 SPPF 替换为带2D sin-cos位置编码的Transformer自注意力。

**文件修改对照表**:

| 配置文件 | 模块 | 改动 |
|----------|------|------|
| `yolo26-RGBT-midfusion-AIFI.yaml` | AIFI Transformer | 修改1行 |

**Backbone改动**:

```
# 原 (行49):
- [-1, 1, SPPF, [1024, 5, 3, True]]  # 24-SPPF

# 新 (行49):
- [-1, 1, Att_AIFI, [1024, 8, 0.0]]  # 24-AIFI: 带位置编码的全局自注意力
```

**Head无改动**: AIFI与SPPF输入输出维度一致，所有Head引用保持不变。

### 代码文件修改

| 文件 | 改动 |
|------|------|
| `ultralytics/nn/ExtraModules/common.py` | 末尾追加 Att_AIFI 和 Att_ScalSeq 两个类，带 `(2026-05-14)` 日期标记 |
| `ultralytics/nn/tasks.py` | base_modules 追加 Att_AIFI, Att_ScalSeq 注册，带日期和功能注释 |
| `train_RGBT_midfuse_testAddModules_withResults.py` | 新建，训练+验证一体化脚本 |

### AIFI 架构示意

```
        P5 Concat (可见光 + 红外)
              |
    ┌─────────────────────────┐
    │   Att_AIFI (替换SPPF)    │  ← MultiheadAttention + FFN
    │   2D sin-cos 位置编码    │     全局自注意力建模
    │   [B,C,H,W]→[B,HW,C]→... │
    └─────────────────────────┘
              |
           C2PSA
              |
        FPN Neck → Detect
```

### yolo-improve 筛选结果

| 模块 | 决定 | 原因 |
|------|------|------|
| **AIFI** | ✅ 纳入 | Transformer自注意力，与现有模块互补 |
| ScalSeq | ⚠️ 待评估 | 3D卷积多尺度融合，需Neck重构 |
| CAM | ❌ 跳过 | 与已有 ASPP_Branch 功能重叠 |
| IoU变体 | ❌ 跳过 | 损失函数改动，侵入性强 |
| aLRPLoss | ❌ 跳过 | 自定义autograd，侵入性强 |
| attention_model | ❌ 跳过 | 与已有 Att_ECA + Att_Coord 重叠 |

### 新增 Plug-and-play 模块 (4个)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-14/`

**文件修改对照表**:

| 配置文件 | 模块 | 改动 |
|----------|------|------|
| `yolo26-RGBT-midfusion-AFF.yaml` | AFF 注意力融合 | 修改3行(Concat→Att_AFF) |
| `yolo26-RGBT-midfusion-iAFF.yaml` | iAFF 迭代融合 | 修改3行(Concat→Att_iAFF) |
| `yolo26-RGBT-midfusion-ULSAM.yaml` | ULSAM 子空间注意力 | 新增3行 + 修改5行 |
| `yolo26-RGBT-midfusion-StripPool.yaml` | StripPool 条带池化 | 修改1行(SPPF→StripPool) |

**AFF/iAFF 改动详情** (Concat → Att_AFF):
```
# P3: [[6, 16], 1, Concat, [1]]    → [[6, 16], 1, Att_AFF, [512]]
# P4: [[8, 18], 1, Concat, [1]]    → [[8, 18], 1, Att_AFF, [512]]
# P5: [[10, 20], 1, Concat, [1]]   → [[10, 20], 1, Att_AFF, [1024]]
```
输出通道与Concat一致，Head全部引用不变。

**StripPool 改动详情** (SPPF → StripPool):
```
# 原: [-1, 1, SPPF, [1024, 5, 3, True]]
# 新: [-1, 1, Att_StripPool, [1024]]
```

### 代码文件修改 (2026-05-14)

| 文件 | 改动 |
|------|------|
| `common.py` | 末尾追加 Att_AFF, Att_iAFF, Att_ULSAM, Att_StripPool 四个类 |
| `tasks.py` | parse_model 新增 Att_AFF/Att_iAFF 多输入处理 + base_modules 注册4个模块 |

### Plug-and-play 筛选结果

| 模块 | 决定 | 原因 |
|------|------|------|
| **AFF / iAFF** | ✅ 纳入 | 专门为特征融合设计，替代Concat智能融合双模态 |
| **ULSAM** | ✅ 纳入 | 超轻量子空间注意力，几乎零参数 |
| **StripPool** | ✅ 纳入 | 1D条带池化长程依赖，对细长IR目标友好 |
| Non-local | ❌ 跳过 | 与已有 AIFI 重叠 (O(N²)注意力) |
| CCNet / DANet | ❌ 跳过 | O(N²)或自定义CUDA，太重 |
| scSE / cSE / sSE | ❌ 跳过 | 与已有 SEAttention / CBAM 重叠 |
| SplitAttention | ❌ 跳过 | 重量级Conv替换 |
| GhostModule | ❌ 跳过 | Ultralytics已内置GhostConv |
| DynamicConv / DGC | ❌ 跳过 | 太复杂，侵入训练循环 |
| SPConv | ❌ 跳过 | 源码有bug(未定义变量) |
| BorderAlignment | ❌ 跳过 | 需要自定义CUDA扩展 |
| PSPModule / PyConv / RFB | ❌ 跳过 | 与已有 ASPP / SPPF 重叠 |

---

## 2026-05-19

### 新增 CMFADet 模块 (4个)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-19/`
> 来源: [CMFADet](https://github.com/Yooyoo95/CMFADet) — Cross-Modality Feature Adaptive Interaction for RGB-IR Detection
> 基线模板: `ultralytics/cfg/models/26-RGBT/yolo26-RGBT-midfusion.yaml`

基于基线模板，引入 CMFADet 的4个核心模块，分别增强 RGB 分支、IR 分支、双模态融合、以及基础卷积。

**文件修改对照表**:

| 配置文件 | 模块 | 改动说明 |
|----------|------|----------|
| `yolo26-RGBT-midfusion-CIFusion.yaml` | Att_CIFusion | 3处 Concat → Att_CIFusion (交叉交换通道注意力融合) |
| `yolo26-RGBT-midfusion-SFEM_IRAFAB.yaml` | Att_SFEM + Att_IRAFAB | RGB-P3 C3k2→Att_SFEM, IR-P3 C3k2→Att_IRAFAB |
| `yolo26-RGBT-midfusion-CMFADet.yaml` | 全部4个模块 | CIFusion + SFEM + IRAFAB + Conv_DE 全集成 |

### CIFusion 配置详情

**Backbone改动** (3处替换):

```
# P3: [[6,16], 1, Concat, [1]]     → [[6,16], 1, Att_CIFusion, [512]]
# P4: [[8,18], 1, Concat, [1]]     → [[8,18], 1, Att_CIFusion, [512]]
# P5: [[10,20], 1, Concat, [1]]    → [[10,20], 1, Att_CIFusion, [1024]]
```

输出通道与Concat一致，Head全部引用不变，无层号位移。

### SFEM + IRAFAB 配置详情

**Backbone改动** (2处替换):

```
# RGB-P3 (行24): C3k2 → Att_SFEM
#  原: [-1, 2, C3k2, [256, False, 0.25]]
#  新: [-1, 2, Att_SFEM, [256, False, 1, 0.25]]     # (c2, shortcut, g, e)

# IR-P3 (行35): C3k2 → Att_IRAFAB
#  原: [-1, 2, C3k2, [256, False, 0.25]]
#  新: [-1, 2, Att_IRAFAB, [256, False, 0.25, 1, True]]  # (c2, c3k, e, g, shortcut)
```

层号不变 (SFEM/IRAFAB 仅改变 C3k2 参数结构，不增减层)，Head无改动。

### CMFADet 全集成配置详情

**Backbone改动** (4类):

```
# 1. RGB-P3: C3k2 → Att_SFEM (频域+空间双路径增强, RGB专用)
# 2. IR-P3:  C3k2 → Att_IRAFAB (自适应多核DWConv + ConvGLU, IR专用)
# 3. P3/P4/P5融合: Concat → Att_CIFusion (交叉交换通道注意力)
# 4. 所有Conv (可选): Conv → Conv (5路边缘感知卷积,训练并行推理融合)
#    注: CMFADet全集成YAML中Conv保留为普通Conv, Conv_DE需手动替换
```

层号不变 (替换模块不增减层)，Head无改动。

### 架构示意 (CMFADet)

```
        Visible Branch                    Infrared Branch
             |                                  |
          Conv (P1)                         Conv (P1)
             |                                  |
          Conv (P2)                         Conv (P2)
             |                                  |
    ┌── Att_SFEM (P3) ← NEW           Att_IRAFAB (P3) ← NEW ──┐
    │    FFT频域+Scharr空间              自适应多核DWConv       │
    │        |                                  |              │
    │      Conv (P4)                        Conv (P4)          │
    │        |                                  |              │
    │      C3k2 (P4)                        C3k2 (P4)          │
    │        |                                  |              │
    │      Conv (P5)                        Conv (P5)          │
    │        |                                  |              │
    │      C3k2 (P5)                        C3k2 (P5)          │
    │        |                                  |              │
    │      Conv (P6)                        Conv (P6)          │
    │        |                                  |              │
    │      C3k2 (P6)                        C3k2 (P6)          │
    └────────┼──────────────────────────────────┼──────────────┘
             └────────── Att_CIFusion ← NEW ────┘
                   交叉交换通道注意力融合
                            |
                          SPPF
                            |
                         C2PSA
                            |
                      Neck → Detect
```

### 代码文件修改 (CMFADet)

| 文件 | 改动 |
|------|------|
| `ultralytics/nn/ExtraModules/fusion/CIFusion.py` | **新建**，CIFusion核心: 交叉交换+通道注意力+门控融合 |
| `ultralytics/nn/ExtraModules/attention/SFEM.py` | **新建**，SFEM核心: FFT频域+Scharr空间+C2f结构 |
| `ultralytics/nn/ExtraModules/attention/IR_AFAB.py` | **新建**，IR_AFAB核心: 自适应多核DWConv+ConvGLU+C3k2结构 |
| `ultralytics/nn/ExtraModules/conv/DEConv.py` | **新建**，DEConv核心: 5路边缘感知卷积(训练并行推理融合) |
| `ultralytics/nn/ExtraModules/common.py` | 第424-493行，新增 `Att_CIFusion`(L433)、`Att_SFEM`(L456)、`Att_IRAFAB`(L467)、`Conv_DE`(L478) 四个包装类 |
| `ultralytics/nn/tasks.py` | 第1071-1076行，`base_modules` 追加 `Conv_DE, Att_CIFusion, Att_SFEM, Att_IRAFAB`；第1103行 `repeat_modules` 追加 `Att_SFEM, Att_IRAFAB`；第1121行多输入模块列表追加 `Att_CIFusion` |
| `ultralytics/nn/ExtraModules/__init__.py` | 第26-30行，新增 `SFEM`, `IR_AFAB`, `CIFusion`, `DEConv` 导入 |

---

### 新增 ICAFusion 模块 (1个)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-19/`
> 来源: [ICAFusion](https://github.com/chanchanchan97/ICAFusion) — Iterative Cross-Attention Guided Feature Fusion for Multispectral Object Detection
> 基线模板: `ultralytics/cfg/models/26-RGBT/yolo26-RGBT-midfusion.yaml`

基于基线模板，将 P3/P4/P5 的 Concat 替换为 Att_ICAFusion (迭代交叉注意力Transformer融合)。

**文件修改对照表**:

| 配置文件 | 模块 | 改动说明 |
|----------|------|----------|
| `yolo26-RGBT-midfusion-ICAFusion.yaml` | Att_ICAFusion | 3处 Concat → Att_ICAFusion (交叉Transformer融合) |

**Backbone改动** (3处替换):

```
# P3: [[6,16], 1, Concat, [1]]     → [[6,16], 1, Att_ICAFusion, [512]]
# P4: [[8,18], 1, Concat, [1]]     → [[8,18], 1, Att_ICAFusion, [512]]
# P5: [[10,20], 1, Concat, [1]]    → [[10,20], 1, Att_ICAFusion, [1024]]
```

输出通道与Concat一致，Head全部引用不变，无层号位移。

### ICAFusion 核心架构

```
   RGB Feature (H×W×C)              IR Feature (H×W×C)
         |                                |
   AdaptivePool2d                  AdaptivePool2d
   (avg+max 可学习加权)            (avg+max 可学习加权)
         |                                |
   位置编码 (sin-cos)              位置编码 (sin-cos)
         |                                |
   ┌─────────────────────────────────────────┐
   │       CrossTransformerBlock × N         │
   │  ┌──────────────────────────────────┐   │
   │  │   CrossAttention                 │   │
   │  │   IR_Q × VIS_K → VIS attention   │   │
   │  │   VIS_Q × IR_K → IR attention    │   │
   │  └──────────────────────────────────┘   │
   │  + MLP (GELU) + 8个LearnableCoefficient │
   └─────────────────────────────────────────┘
         |                                |
   Upsample + Skip Connect        Upsample + Skip Connect
         |                                |
   Concat(2C) → 1×1Conv → C  (拼接投影)
         |
    融合特征输出
```

### 代码文件修改 (ICAFusion)

| 文件 | 改动 |
|------|------|
| `ultralytics/nn/ExtraModules/fusion/ICAFusion.py` | **新建**，226行。核心类: `AdaptivePool2d`, `LearnableCoefficient`, `LearnableWeights`, `CrossAttention`, `CrossTransformerBlock`, `TransformerFusionBlock` |
| `ultralytics/nn/ExtraModules/common.py` | 第496-523行，新增 `Att_ICAFusion` 包装类，channel对齐1x1Conv + ICAFusionCore |
| `ultralytics/nn/tasks.py` | 第1078-1080行，`base_modules` 追加 `Att_ICAFusion`；第1121行多输入模块列表追加 `Att_ICAFusion` |
| `ultralytics/nn/modules/conv.py` | 第474-828行，追加 ICAFusion 完整源码副本 (~354行，含 `NiNfusion`, `AdaptivePool2d`, `CrossAttention`, `CrossTransformerBlock`, `TransformerFusionBlock` 等) |
| `ICAFusion-main/` | 根目录下原始ICAFusion源码仓库 (~30个文件) |

### CMFADet vs ICAFusion 对比

| 维度 | CMFADet (CIFusion) | ICAFusion |
|------|-------------------|-----------|
| 融合机制 | 交叉交换+通道注意力 (CNN) | 交叉注意力+MLP (Transformer) |
| 参数量 | 轻量 (~0.1M) | 较重 (含MultiheadAttention) |
| 专用模块 | SFEM(RGB) + IRAFAB(IR) + DEConv | 无 |
| 位置编码 | 无 | 可学习sin-cos 2D |
| 适用场景 | 轻量级双模态交互 | 全局跨模态上下文建模 |

---

### 训练/检测脚本合并重构

> 目的: 消除功能重复的脚本，用 argparse 统一接口

**删除的冗余文件 (7个)**:

| 删除文件 | 原因 |
|----------|------|
| `train_AddModules_resume.py` | 已合并到 `train_AddModules.py --resume` |
| `train_resume.py` | 功能完全被 `train_AddModules.py --resume` 覆盖 |
| `val_zjy.py` | `val_zjy_full.py` 的硬编码子集 |
| `detect-1C.py` | 合并到 `detect.py --use_simotm Gray --channels 1` |
| `detect-4C.py` | 合并到 `detect.py --use_simotm RGBT --channels 4` |
| `detect-6C.py` | 合并到 `detect.py --use_simotm RGBRGB6C --channels 6` |
| `detect-multispectral.py` | 合并到 `detect.py --use_simotm Multispectral_16bit --channels 7` |

**修改的文件 (3个)**:

| 文件 | 改动 | 行数变化 |
|------|------|----------|
| `train_AddModules.py` | 新增 `DEFAULT_RESUME` 和 `--resume` 参数，支持从头训练和断点续训两种模式；续训时自动从checkpoint恢复模型+优化器状态 | 294→317 |
| `detect.py` | 改为 argparse 命令行接口，`--use_simotm` 覆盖8种模态，`--channels` 支持1~10通道，新增 `--task` (detect/obb等)、`--conf`、`--visualize` 等参数 | 24→77 |
| `train_RGBT_mine_print.py` | 添加详细中文注释: 模块说明、model.info()参数说明、完整的训练配置模板 | 28→70 |

**保留的独立脚本 (不变)**:
`train.py` (简单入口)、`train-cls.py` (分类任务)、`train-rtdetr.py` (RTDETR模型)、`train_Gray.py` (灰度)、`train_MCF_demo.py` (MCF多步流程)、`train_multispectral.py` (多光谱)、`val.py`、`val_zjy_full.py`

---

## 2026-05-26

### 新增 ATAH 检测头模块

> 来源: CMFADet — Adaptive Task-aware Alignment Head
> 原始依赖: mmcv (ModulatedDeformConv2d + build_norm_layer)
> 移植依赖: torchvision.ops.deform_conv2d (纯 PyTorch, 无需 mmcv)

**改动说明**:

| 维度 | 原版 CMFADet ATAH | 移植版 |
|------|------------------|--------|
| 可变形卷积 | mmcv.ModulatedDeformConv2d | torchvision.ops.deform_conv2d |
| 归一化层 | mmcv.build_norm_layer | nn.GroupNorm(16, out_channels) |
| 输入对齐 | 无 (依赖YAML统一通道) | 新增1x1投影层, P3/P4/P5→hidc |
| 依赖 | mmcv-full | 纯 torch + torchvision |

**核心架构**:

```
   P3(31)  P4(34)  P5(37)
     |       |       |
  1x1Proj 1x1Proj 1x1Proj  ← 通道对齐到 hidc
     |       |       |
   share_conv (hidc → hidc//2 → hidc//2)  ← 共享特征提取
     |       |       |
   ┌─cls_branch(TaskAwareFeatureMod)  ← 分类: 全局池化→降维→Sigmoid门控
   └─reg_branch(TaskAwareFeatureMod)  ← 回归: 同上
              |
         DyDCNv2 (可变形卷积 v2)     ← offset+mask 空间对齐
              |
         cv2(reg_feat×scale) + cv3(cls_feat×cls_prob)
              |
         [B, 4*reg_max+nc, H, W]     ← 输出
```

**代码文件修改**:

| 文件 | 改动 |
|------|------|
| `ultralytics/nn/ExtraModules/head/ATAH.py` | **新建**，~170行。包含 `Scale`, `Conv_GN`, `TaskawareFeatureModulator`, `DyDCNv2`, `Detect_ATAH` 五个类 |
| `ultralytics/nn/ExtraModules/head/__init__.py` | **新建**，`from .ATAH import *` |
| `ultralytics/nn/ExtraModules/__init__.py` | 第32行，新增 `from .head import *` |
| `ultralytics/nn/tasks.py` | 第1124行新增 `Detect_ATAH` 特殊处理 (args.append) ; 第296/362行 isinstance 检查追加 `Detect_ATAH` |

**Detect_ATAH vs Detect 对比**:

| 维度 | Detect | Detect_ATAH |
|------|--------|-------------|
| 参数 | `Detect, [nc]` | `Detect_ATAH, [nc, hidc]` |
| 分支 | 独立 conv per layer | 共享 share_conv + 任务感知调制 |
| 空间对齐 | 无 | DyDCNv2 可变形卷积 |
| 分类约束 | 无 | 空间概率图 cls_prob |
| 回归缩放 | 无 | 可学习 Scale per layer |

---

### CMFADet 消融实验 YAML 配置

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-19/CMFADet=SFEM+IRAFAB+CIF+ATAH/`
> 基线: `yolo26-RGBT-midfusion.yaml` (无任何 CMFADet 模块)
> 所有配置层号完全一致 (替换模块不增减层), 确保对比严谨

**消融实验矩阵**:

| 编号 | YAML | SFEM | IR_AFAB | CIFusion | ATAH | 说明 |
|------|------|------|---------|----------|------|------|
| Baseline | `baseline` | | | | | 对照基础 |
| (a) | `a-SFEM` | ✓ | | | | 仅 RGB分支空间-频率增强 |
| (b) | `b-IRAFAB` | | ✓ | | | 仅 IR分支自适应聚合 |
| (c) | `c-CIF` | | | ✓ | | 仅跨模态交叉交换融合 |
| (d) | `d-ATAH` | | | | ✓ | 仅任务感知对齐检测头 |
| (e) | `e-SFEM+IRAFAB` | ✓ | ✓ | | | 骨干非对称增强 |
| (f) | `f-SFEM+IRAFAB+CIF` | ✓ | ✓ | ✓ | | 骨干+融合 |
| (g) | `g-SFEM+IRAFAB+CIF+ATAH` | ✓ | ✓ | ✓ | ✓ | 完整 CMFADet |

**实验设计逻辑**:

```
Baseline ──────────────────────────────────────────→ 对照组
  ├─ (a) SFEM        ──→ 验证RGB空间频域增强贡献
  ├─ (b) IR_AFAB     ──→ 验证IR自适应特征聚合贡献
  ├─ (c) CIFusion    ──→ 验证跨模态交叉融合贡献
  ├─ (d) ATAH        ──→ 验证任务感知检测头贡献
  ├─ (e) a+b         ──→ 验证骨干非对称增强协同
  ├─ (f) e+c         ──→ 验证骨干+融合递进收益
  └─ (g) f+d         ──→ 验证全模块最优组合
```

**具体改动位置** (所有YAML层号一致):

| 模块 | 层号 | Baseline | 激活时替换 |
|------|------|----------|-----------|
| SFEM | 4 | `C3k2, [256,False,0.25]` | `Att_SFEM, [256,False,1,0.25]` |
| IR_AFAB | 14 | `C3k2, [256,False,0.25]` | `Att_IRAFAB, [256,False,0.25,1,True]` |
| CIFusion | 21,22,23 | `Concat, [1]` | `Att_CIFusion, [512/512/1024]` |
| ATAH | 38 | `Detect, [nc]` | `Detect_ATAH, [nc,256]` |

---

## 2026-05-27

### 大规模模块集成 (来自 objectdetection_script 扫描)

扫描了 `objectdetection_script/` 的三个子目录：
- **module-info/** — 30篇顶会论文模块 (CVPR/ICLR/TPAMI)
- **yolo-improve/** — 剩余未处理模块 (~40个)
- **objectdetection-tricks/** — 16个实用训练/调试技巧

分四批集成共计 20 个新模块 + 7 个工具，按功能分 7 个子目录存放。

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-05-27/`

---

### 一、新增 ExtraModules 子目录结构

```
ExtraModules/
├── conv/     ← CoordConv, PConv, TridentBlock, StarBlock (4个)
├── dynamic/  ← DynamicConv (1个)
├── context/  ← EVC, ContextAgg (2个)
├── neck/     ← CARAFE, ASF (2个)
├── head/     ← DecoupledHead (1个, 追加到已有)
├── frequency/← FDConv, vHeat, TOST, FADC, EBlock/DBlock, SFSConv (6个)
├── mamba/    ← EfficientViM_Block, WTE_Mamba, mamba_utils (3个)
├── loss/     ← NWDLoss, SoftNMS (2个, 独立使用不修改源码)
tools/         ← 5个实用脚本 + README (项目根目录)
```

### 二、Batch 1: yolo-improve 模块 (10个)

**代码文件修改表**:

| 文件 | 改动 |
|------|------|
| `conv/CoordConv.py` | 新建，`AddCoords` 附加归一化 x/y 坐标通道；`Conv_Coord` 带 CoordConv 预处理的 Conv 包装 |
| `conv/PConv.py` | 新建，`PConv` 仅对 1/4 通道做 3×3 卷积其余恒等；`Faster_Block` PConv+MLP 轻量瓶颈 |
| `conv/TridentBlock.py` | 新建，`TridentBlock` 共享权重 3 种空洞率(1,2,3)并行；`RFEM` 多空洞融合；`C3_RFEM` C3 包装 |
| `conv/StarBlock.py` | 新建，`StarBlock` 星操作：`ReLU6(b1) * b2` 逐元素乘法隐式高维特征 |
| `dynamic/DynamicConv.py` | 新建，`Dynamic_conv2d` K=4 专家卷积核 + 温度 softmax 注意力路由 |
| `context/EVC.py` | 新建，`LVCBlock` 可学习视觉码本(缩放L2距离+softmax分配)；`LightMLPBlock` DWConv+MLP；`EVCBlock` 双分支融合 |
| `context/ContextAgg.py` | 新建，`ContextAggregation` 非局部全局上下文聚合(~8K参数，无mmcv依赖) |
| `neck/CARAFE.py` | 新建，`CARAFE` 内容感知重组核上采样(压缩→编码→PixelShuffle→加权组合)；`CARAFE_Upsample` 自动推断通道包装 |
| `neck/ASF.py` | 新建，`Zoom_cat` 3尺度自适应池化/插值拼接；`ScalSeq` 3D卷积跨尺度交互；`attention_model` ECA通道+坐标注意力；`ASF_fusion` 组合模块 |
| `head/DecoupledHead.py` | 新建，`DecoupledHead` 分类/回归分支解耦检测头，含 DFL + TAL 推理路径，1×1投影对齐 P3/P4/P5 通道 |

**tasks.py 注册**:

```python
# base_modules 新增:
Conv_Coord, PConv, Faster_Block, C3_RFEM, StarBlock,
Dynamic_conv2d, EVCBlock, ContextAggregation,
CARAFE, CARAFE_Upsample, ASF_fusion, Zoom_cat, ScalSeq, attention_model

# repeat_modules 新增:
C3_RFEM

# parse_model 多输入分支扩展:
if m in (..., ASF_fusion, Zoom_cat, ScalSeq, attention_model)

# parse_model 检测头分支扩展:
elif m in (Detect_ATAH, DecoupledHead)
```

**YAML 配置表 (conv/)**:

| 配置文件 | 模块 | 改动位置 |
|----------|------|----------|
| `conv/yolo26-RGBT-midfusion-Conv_Coord.yaml` | Conv_Coord | 10处 backbone Conv → Conv_Coord (层2,3,5,7,9,12,13,15,17,19) |
| `conv/yolo26-RGBT-midfusion-PConv.yaml` | PConv | 2处 neck Conv → PConv (层32,35) |
| `conv/yolo26-RGBT-midfusion-Faster_Block.yaml` | Faster_Block | RGB-P3/IR-P3 C3k2 → Faster_Block (层4,14) |
| `conv/yolo26-RGBT-midfusion-C3_RFEM.yaml` | C3_RFEM | RGB-P3/IR-P3 C3k2 → C3_RFEM (层4,14) |
| `conv/yolo26-RGBT-midfusion-StarBlock.yaml` | StarBlock | RGB-P4/IR-P4 C3k2 → StarBlock (层6,16) |

**YAML 配置表 (dynamic/)**:

| 配置文件 | 模块 | 改动位置 |
|----------|------|----------|
| `dynamic/yolo26-RGBT-midfusion-DynamicConv.yaml` | Dynamic_conv2d | 2处 neck Conv → Dynamic_conv2d (层32,35) |

**YAML 配置表 (context/)**:

| 配置文件 | 模块 | 改动位置 | 层号位移 |
|----------|------|----------|----------|
| `context/yolo26-RGBT-midfusion-EVC.yaml` | EVCBlock | SPPF → EVCBlock (层24) | 无 |
| `context/yolo26-RGBT-midfusion-ContextAgg.yaml` | ContextAggregation | SPPF后插入ContextAgg (新层25) | C2PSA+1, head全部+1 |

**YAML 配置表 (neck/)**:

| 配置文件 | 模块 | 改动位置 | 层号位移 |
|----------|------|----------|----------|
| `neck/yolo26-RGBT-midfusion-CARAFE.yaml` | CARAFE_Upsample | 2处 nn.Upsample → CARAFE_Upsample (层26,29) | 无 |
| `neck/yolo26-RGBT-midfusion-ASF.yaml` | ASF_fusion | P3/P4/P5融合后插入ASF_fusion (新层24) | SPPF+1, C2PSA+1, head全部+1 |

**YAML 配置表 (head/)**:

| 配置文件 | 模块 | 改动位置 |
|----------|------|----------|
| `head/yolo26-RGBT-midfusion-DecoupledHead.yaml` | DecoupledHead | Detect → DecoupledHead (层38) |

---

### 三、Batch 2: module-info 频域模块 (6个)

> 说明: module-info/ 仅含论文 .md 摘要，无源代码。以下模块均为基于论文描述的纯 PyTorch 实现。

**代码文件修改表**:

| 文件 | 改动 |
|------|------|
| `frequency/FDConv.py` | 新建，FDConv: FFT→同心圆环频带分解→独立复数调制→IFFT→标准Conv |
| `frequency/vHeat.py` | 新建，vHeat: 2D DCT→预测热扩散系数k(GAP+FC+Sigmoid)→exp(-k·t·(wx²+wy²))衰减→2D IDCT→残差 |
| `frequency/TOST.py` | 新建，TOST: 线性注意力 O(n)→QKV低维投影→K均值统计量→softmax聚合→加权V求和 |
| `frequency/FADC.py` | 新建，FADC: Sobel梯度→频率感知→多膨胀率权重预测→加权融合→高频增强→残差 |
| `frequency/DarkIR.py` | 新建，EBlock: FFT→幅度调制(保留相位)→IFFT→残差；DBlock: 多扩张DWConv+空间注意力 |
| `frequency/SFSConv.py` | 新建，SFSConv: 50/50通道分流→空间分支(多尺度DWConv)+频率分支(Gabor滤波)→CSU自适应融合 |

**tasks.py 注册**:

```python
# base_modules 新增:
FDConv, vHeat, TOST, FADC, EBlock, DBlock, SFSConv
```

**YAML 配置表 (frequency/)**:

| 配置文件 | 模块 | 改动位置 | 层号位移 |
|----------|------|----------|----------|
| `frequency/yolo26-RGBT-midfusion-FDConv.yaml` | FDConv | 10处 backbone Conv → FDConv | 无 |
| `frequency/yolo26-RGBT-midfusion-vHeat.yaml` | vHeat | SPPF → vHeat (层24) | 无 |
| `frequency/yolo26-RGBT-midfusion-TOST.yaml` | TOST | SPPF → TOST (层24) | 无 |
| `frequency/yolo26-RGBT-midfusion-FADC.yaml` | FADC | 2处 neck Conv → FADC (层32,35) | 无 |
| `frequency/yolo26-RGBT-midfusion-EBlock.yaml` | EBlock | IR-P3 C3k2后插入EBlock (新层15) | IR后续全部+1, Concat/head引用更新 |
| `frequency/yolo26-RGBT-midfusion-SFSConv.yaml` | SFSConv | RGB-P3/IR-P3 Conv → SFSConv (层5,15) | 无 |

---

### 四、Batch 3: Mamba/SSM模块 (2个)

> 说明: 纯 PyTorch 实现 SSM，无 mamba-ssm CUDA 依赖。selective scan 使用 for 循环 recurrence `h = dA*h + dB*x`。

**代码文件修改表**:

| 文件 | 改动 |
|------|------|
| `mamba/mamba_utils.py` | 新建，`selective_scan` S6-style RNN recurrence；`bidirectional_selective_scan` 双向扫描+翻转融合；`SSMCore` 完整SSM块 |
| `mamba/EfficientViM.py` | 新建，`EfficientViM_Block` 单头设计 LN→in_proj→conv1d→scan→gate→out_proj+残差；`EfficientViM_HSM` 隐藏状态混合变体 |
| `mamba/MobileMamba.py` | 新建，`HaarWaveletDecompose/Reconstruct` Haar小波4频带分解/重建；`WaveletSSMBranch` 每频带SSM；`MultiKernelDWConv` MK-DeConv；`WTE_Mamba` 三路融合 |

**tasks.py 注册**:

```python
# base_modules 新增:
EfficientViM_Block, WTE_Mamba
```

**YAML 配置表 (mamba/)**:

| 配置文件 | 模块 | 改动位置 |
|----------|------|----------|
| `mamba/yolo26-RGBT-midfusion-EfficientViM.yaml` | EfficientViM_Block | SPPF → EfficientViM_Block (层24) |
| `mamba/yolo26-RGBT-midfusion-WTE_Mamba.yaml` | WTE_Mamba | SPPF → WTE_Mamba (层24) |

**架构示意 (Mamba 替代 SPPF)**:

```
     P5 融合 (Concat layer 23)
              |
    ┌─────────────────────────┐
    │  Mamba/SSM (替换SPPF)    │  ← 双向选择性扫描
    │  [B,C,H,W]→flatten→scan  │     线性复杂度序列建模
    │  forward scan + back scan │     双模态通道不二次爆炸
    └─────────────────────────┘
              |
           C2PSA
              |
        FPN Neck → Detect
```

---

### 五、Batch 4: 实用工具 (7个)

**工具脚本 (tools/)**:

| 文件 | 功能 |
|------|------|
| `tools/dataset_analysis.py` | 数据集小/中/大目标分布统计，支持YOLO和COCO格式 |
| `tools/layer_info.py` | 逐层特征图shape/通道调试，注册forward hooks |
| `tools/flops_analysis.py` | 逐层FLOPs/参数量分析(via thop)，标记超阈值层 |
| `tools/feature_heatmap.py` | 特征图JET热力图可视化，支持选层+保存.npy |
| `tools/error_analysis.py` | TP/FP/FN误差可视化(绿/蓝/红框)，IoU匹配统计 |
| `tools/README.md` | 中文使用说明，含参数表和示例 |

**损失工具 (ExtraModules/loss/)**:

| 文件 | 功能 |
|------|------|
| `loss/NWDLoss.py` | Normalized Wasserstein Distance 小目标回归损失，含手动集成指南 |
| `loss/SoftNMS.py` | 高斯衰减 Soft-NMS 后处理，支持 GIoU/DIoU/CIoU/SIoU/EIoU |

---

### 六、筛选结果

#### module-info 筛选 (30→6)

| 模块 | 决定 | 原因 |
|------|------|------|
| **FDConv, vHeat, TOST, FADC, EBlock, SFSConv** | ✅ 纳入 | 频域/统计机制新颖，与已有模块无重叠，对 RGBT 有明确价值 |
| StarBlocks | ✅ 纳入 | 有源码(yolo-improve)，零成本高阶交互 |
| EfficientViM, MobileMamba | ✅ 纳入 | SSM 全局建模，纯 PyTorch 实现无 CUDA 依赖 |
| DynamicConv/ParameterNet | ✅ 纳入 | 已有 DynamicConv 源码，K=4 专家路由 |
| DarkIR (DBlock) | ✅ 纳入 | FFT 幅度增强对 IR 低对比度输入有直接价值 |
| MambaVision, GroupMamba, OverLoCK, PolaFormer | ❌ 跳过 | 与已纳入 SSM/注意力模块重叠 |
| HyperC2Net, PKI, LS Block, DyT, Mona | ❌ 跳过 | 超图计算复杂/增量改进/微调特定 |
| BHViT, MambaIR, EVSSM, DCMPNet, SCSegamba | ❌ 跳过 | 量化/恢复/分割特定，非检测任务 |

#### yolo-improve 筛选 (40+→10)

| 模块 | 决定 | 原因 |
|------|------|------|
| **CoordConv, PConv, C3_RFEM, StarBlock, DynamicConv, EVC, ContextAgg, CARAFE, ASF, DecoupledHead** | ✅ 纳入 | 机制新颖，轻量级，纯 PyTorch |
| CAM, IoU变体, aLRPLoss | ❌ 跳过 | 与已有重叠/侵入性强 |
| DyHead, DySnakeConv, ODConv, SAConv, DBB | ❌ 跳过 | 需mmcv/太重/实现复杂 |
| 各种 backbone 替换 | ❌ 跳过 | 完整网络替换，非即插即用模块 |
| RepNCSPELAN, GFPN, GOLD-YOLO | ❌ 跳过 | 完整颈部替换，侵入性强 |
| TSCODE | ❌ 跳过 | 需einops，5输入检测头解析复杂 |
| Res2Block | ❌ 跳过 | print(1)调试语句，代码质量问题 |
| Soft-NMS, NWD | ✅ 纳入 | 作为独立工具，不修改源码 |

---

### 七、不修改源码说明

所有 20 个模块均为**纯追加**：
- 新建独立 .py 文件 → OK
- `__init__.py` 末尾追加 import → OK
- `tasks.py` 末尾追加 base_modules/repeat_modules/parse_model → OK
- **未修改任何 ultralytics 核心逻辑**（engine/, utils/loss.py, models/yolo/ 等）

NWDLoss 和 SoftNMS 放在 `ExtraModules/loss/` 作为独立工具，不会自动注入训练流程。如需在训练中使用，需手动在 `ultralytics/utils/loss.py` 中挂载——**这是唯一需要手动修改源码的地方**。

---

## 2026-06-04

### 从 vHeat-main 提取 vHeat_Block + vHeat_Fusion

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-04/`

**筛选结果**:

| 模块 | 决定 | 原因 |
|------|------|------|
| vHeat_Block | ✅ 纳入 | 原论文完整 HeatBlock(HCO+MLP+DropPath+LayerScale)，比简化版vHeat功能完备 |
| vHeat_Fusion | ✅ 纳入(创新) | 热传导原理首次应用于RGB-T融合，频域天然适配双模态互补，论文级创新点 |
| 完整vHeat backbone | ❌ 跳过 | 需要替换整个backbone+mmdet，非即插即用 |

**文件修改对照表**:

| 配置/代码 | 改动 |
|-----------|------|
| `frequency/yolo26-RGBT-midfusion-vHeat_Block.yaml` | 层24 SPPF → vHeat_Block，原位替换 |
| `fusion/yolo26-RGBT-midfusion-vHeat_Fusion.yaml` | 层21/22/23 Concat → vHeat_Fusion，原位替换 |

**代码文件修改表**:

| 文件 | 行号 | 改动 |
|------|------|------|
| `ultralytics/nn/ExtraModules/__init__.py` | 61后, 28后 | 追加 vHeat_Block 和 vHeat_Fusion 导入 |
| `ultralytics/nn/tasks.py` | ~1113 (base_modules) | 追加 vHeat_Block, vHeat_Fusion |
| `ultralytics/nn/tasks.py` | ~1176 (parse_model) | 多输入分支追加 vHeat_Fusion |
| `ultralytics/nn/ExtraModules/frequency/vHeat.py` | 多处 | register_buffer → 普通dict (EMA兼容性修复) |

**vHeat_Block 模型参数**:
- YOLO26s-midfusion-vHeat_Block: 463 layers, 17.21M params, 35.99 GFLOPs
- vs 基线 (319 layers, 9.46M, 21.7 GFLOPs): +7.8M params, +14.3 GFLOPs

**vHeat_Fusion 模型参数**:
- YOLO26s-midfusion-vHeat_Fusion: 521 layers, 21.53M params, 47.07 GFLOPs
- vs 基线: +12.1M params, +25.4 GFLOPs

**训练冒烟测试**: ✓ 通过 (FLIR 2 epochs, imgsz=320, batch=2, device=0, GPU_mem=1.28G, loss从6.5稳定下降到4.15, 无OOM/NaN/报错)
**训练环境**: D:\Anaconda\envs\torch310 (Python 3.10, torch 2.2.2+cu121)

---

## 2026-06-20

### 新增 FCMMFusion 跨分支交叉门控融合模块 (源自 FCM-main)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-20/fusion/`
> 来源: FCM-main/ultralytics/nn/modules/conv.py 中 FCM 系列模块的思想改造

**筛选分析**:

| 模块 | 决定 | 原因 |
|------|------|------|
| FCM / FCM_1 / FCM_2 / FCM_3 | ⚠️ 纳入(改造) | 双分支Cross-Gating机制有融合价值,但原模块是单输入,需改造为双模态融合 |
| Pzconv | ❌ 跳过 | 多核串行DWConv+残差,创新性一般,与已有DEConv重叠 |
| Down | ❌ 跳过 | groups=dim//2写死,可移植性差 |
| Channel / Spatial | ⚠️ 纳入(改造) | 作为FCMMFusion的门控子模块,从原仓库独立出来整合 |

**FCM-main 代码质量问题** (改造时已修复):
1. `conv.py` 重复定义 `DWConv` 类名,后者覆盖前者
2. `__all__` 和 `__init__` 导入了 `einops.rearrange` 但代码未使用,多余依赖
3. `Down` 中 `groups=dim // 2` 硬编码,对奇数通道不兼容
4. 所有模块均无中文注释,docstring 缺失

**改造方案**: 将 FCM 的双分支 Cross-Gating 思想改造为双输入融合模块:
- 原 FCM: 单输入 → split → 两路处理 → 空间/通道门控交叉 → 融合
- 改造后: 双输入 [vis, ir] → 各自Proj → vis用ir空间门控 + ir用vis通道门控 → 融合

**文件修改对照表**:

| 配置/代码 | 改动 |
|-----------|------|
| `fusion/FCMMFusion.py` | 新建,FCMMFusion + FCMBlockFusion 两个类 |
| `fusion/yolo26-RGBT-midfusion-FCMMFusion.yaml` | 层21/22/23 Concat → FCMMFusion,原位替换 |

**代码文件修改表**:

| 文件 | 行号 | 改动 |
|------|------|------|
| `ultralytics/nn/ExtraModules/fusion/FCMMFusion.py` | 新建 | FCMMFusion + FCMBlockFusion 实现 |
| `ultralytics/nn/ExtraModules/__init__.py` | 28后 | 追加 `from .fusion.FCMMFusion import *` |
| `ultralytics/nn/tasks.py` | ~1116 (base_modules) | 追加 FCMMFusion, FCMBlockFusion |
| `ultralytics/nn/tasks.py` | ~1183 (parse_model) | 多输入分支追加 FCMMFusion, FCMBlockFusion |

**FCMMFusion 模型参数**:
- YOLO26s-midfusion-FCMMFusion: 491 layers, 19.96M params, 47.63 GFLOPs
- vs 基线 (319 layers, 9.46M, 21.7 GFLOPs): +10.5M params, +25.9 GFLOPs

**训练环境**: D:\Anaconda\envs\torch310 (Python 3.10, torch 2.2.2+cu121)

---

## 2026-06-21

### 新增 EI2Det 创新点改造模块 (4个融合模块, 源自 EI2Det-master)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-21/fusion/`
> 来源: EI2Det-master (Emerging Image Dual-modal Detection)

**EI2Det 核心创新点分析**:

| # | 创新点 | 原始模块 | 核心机制 | 移植决策 |
|---|--------|----------|----------|----------|
| 1 | DMAF 差分模态感知融合 | DMAF | 差分图GAP+tanh交叉加权 | ✅ 纳入(改造) |
| 2 | IWM 天气感知加权 | IWM | 原始图像→CNN→天气权重 | ✅ 纳入(改造为QualityWeightedFusion) |
| 3 | EFM 边缘混合 | EFM | Sobel边缘检测+可学习alpha混合 | ✅ 纳入(改造为EdgeBlendFusion) |
| 4 | TransformerFusionBlock | 全流程 | 池化→交叉注意力→天气加权→边缘混合 | ✅ 纳入(改造为EI2Fusion) |
| 5 | CrossTransformerBlock+LearnableCoefficients | 交叉注意力 | 8个可学习系数控制残差 | ❌ 跳过(与ICAFusion重叠) |

**文件修改对照表**:

| 配置/代码 | 改动 |
|-----------|------|
| `fusion/DMAF.py` | 新建, 差分模态感知融合 |
| `fusion/EI2Fusion.py` | 新建, QualityWeightedFusion+EdgeBlendFusion+EI2Fusion |
| `fusion/yolo26-RGBT-midfusion-DMAF.yaml` | 层21/22/23 Concat → DMAF, 原位替换 |
| `fusion/yolo26-RGBT-midfusion-QualityWeightedFusion.yaml` | 层21/22/23 Concat → QualityWeightedFusion, 原位替换 |
| `fusion/yolo26-RGBT-midfusion-EdgeBlendFusion.yaml` | 层21/22/23 Concat → EdgeBlendFusion, 原位替换 |
| `fusion/yolo26-RGBT-midfusion-EI2Fusion.yaml` | 层21/22/23 Concat → EI2Fusion, 原位替换 |

**代码文件修改表**:

| 文件 | 行号 | 改动 |
|------|------|------|
| `ultralytics/nn/ExtraModules/fusion/DMAF.py` | 新建 | DMAF融合模块 |
| `ultralytics/nn/ExtraModules/fusion/EI2Fusion.py` | 新建 | QualityWeightedFusion+EdgeBlendFusion+EI2Fusion |
| `ultralytics/nn/ExtraModules/__init__.py` | 末尾 | 追加 `from .fusion.DMAF import *` 和 `from .fusion.EI2Fusion import *` |
| `ultralytics/nn/tasks.py` | ~base_modules | 追加 DMAF, QualityWeightedFusion, EdgeBlendFusion, EI2Fusion |
| `ultralytics/nn/tasks.py` | ~parse_model多输入 | 追加 DMAF, QualityWeightedFusion, EdgeBlendFusion, EI2Fusion |

**模型参数对比**:

| 配置 | layers | params | GFLOPs | vs基线(9.46M/21.7G) |
|------|--------|--------|--------|---------------------|
| DMAF | 470 | 15.63M | 42.29G | +6.17M / +20.59G |
| QualityWeightedFusion | 488 | 16.03M | 42.61G | +6.57M / +20.91G |
| EdgeBlendFusion | 470 | 16.42M | 46.68G | +6.96M / +24.98G |
| EI2Fusion | 491 | 16.81M | 47.01G | +7.35M / +25.31G |
| FCMMFusion(2026-06-20) | 491 | 19.96M | 47.63G | +10.5M / +25.93G |
## 2026-06-21

### 新增 11 个模块 (来自 yoloair-main)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-21/`
> 来源: yoloair-main (positive666/yolov5_research + iscyy/yoloair)

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| CrissCrossAttention | ✅ 纳入 | 高效non-local, RGB-T跨模态空间建模 |
| SOCA | ✅ 纳入 | 二阶通道注意力, 高阶统计建模 |
| SKAttention | ✅ 纳入 | 多尺度感受野自适应 |
| NAMAttention | ✅ 纳入 | 极轻量BN统计注意力 |
| S2Attention | ✅ 纳入 | 零参数空间移位 |
| ACmix | ✅ 纳入 | 混合注意力+卷积 |
| BoT3 | ✅ 纳入 | 全局上下文Transformer |
| GSConv/VoVGSCSP | ✅ 纳入 | 轻量级卷积替代 |
| Involution | ✅ 纳入 | 位置感知动态卷积核 |
| ASFFFusion | ✅ 纳入 | 双模态自适应融合(ASFF改造) |
| SPPCSPC/SPPFCSPC | ✅ 纳入 | CSP增强SPP |
| GAMAttention | ⚠️ 跳过 | 已有Att_GAM |
| SKAttention(SKNet) | ⚠️ 重叠 | 与已有SK功能类似, 但实现不同 |
| HorBlock/gnconv | ❌ 跳过 | HorNet系列, 与已有StarBlock重叠 |
| Decoupled_Detect | ❌ 跳过 | 与已有DecoupledHead一致 |

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `attention/CrissCross.py` | 新增 CrissCrossAttention |
| `attention/SOCA.py` | 新增 SOCA |
| `attention/SKAttention.py` | 新增 SKAttention |
| `attention/NAM.py` | 新增 NAMAttention |
| `attention/S2Attention.py` | 新增 S2Attention |
| `attention/ACmix.py` | 新增 ACmix |
| `attention/BoT3.py` | 新增 BoT3 |
| `conv/GSConv.py` | 新增 GSConv, VoVGSCSP, GSBottleneck |
| `conv/Involution.py` | 新增 Involution, InvolutionBottleneck |
| `context/SPPCSPC.py` | 新增 SPPCSPC, SPPFCSPC |
| `fusion/ASFFFusion.py` | 新增 ASFFFusion (双模态版) |
| `__init__.py` | 追加11条import |
| `tasks.py` | base_modules追加11个, 多输入分支追加ASFFFusion |
| `common.py` | 追加Involution包装器 |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | 84-95 | 追加11条from .xxx import * |
| `common.py` | 525-535 | 追加Involution包装器 |
| `tasks.py` | 1148 | base_modules追加11个模块 |
| `tasks.py` | 1191 | 多输入分支追加ASFFFusion |

---

## 2026-06-23

### 新增 LCAFNet 跨模态交叉注意力融合模块

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-23/fusion/`
> 来源: LCAFNet (LCAFNet/models/common.py)

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `fusion/HAFFormer.py` | 新增 Att_HAFFormer, CrossAttention_S, HAFFormerCore |
| `fusion/CrossAttention_M.py` | 新增 Att_CrossAttention_M, CrossAttention_MCore |
| `__init__.py` | 追加2条import |
| `tasks.py` | base_modules追加2个, 多输入分支追加2个 |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | 84 | 追加 `from .fusion.HAFFormer import *` 和 `from .fusion.CrossAttention_M import *` |
| `tasks.py` | ~1165 | base_modules追加 Att_HAFFormer, Att_CrossAttention_M |
| `tasks.py` | ~1207 | 多输入分支追加 Att_HAFFormer, Att_CrossAttention_M |

**HAFFormer 模型参数**:
- YOLO26s-midfusion-HAFFormer: 512 layers, 32.7M params, 90.3 GFLOPs
- vs 基线(SPPF+C2PSA+Concat): +约10M params

**CrossAttention_M 模型参数**:
- YOLO26s-midfusion-CrossAttention_M: 470 layers, 23.2M params, 75.2 GFLOPs
- vs 基线: +约1M params

### 新增 GatherExcite 注意力模块

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-23/attention/`
> 来源: timm GatherExcite, Fracture_Detection_Improved_YOLOv8

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `attention/GatherExcite.py` | 新增 Att_GatherExcite, GatherExciteCore |
| `__init__.py` | 追加1条import |
| `tasks.py` | base_modules追加 Att_GatherExcite |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | 84 | 追加 `from .attention.GatherExcite import *` |
| `tasks.py` | ~1167 | base_modules追加 Att_GatherExcite |

**GatherExcite 模型参数**:
- YOLO26s-midfusion-GatherExcite: 430 layers, 14.3M params, 33.6 GFLOPs
- vs 基线(C2PSA): -约8M params (更轻量)

### 新增 BinaryAttention 1-bit QK注意力模块 (CVPR2026)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-27/attention/`
> 来源: CVPR2026 - BinaryAttention: One-Bit QK-Attention for Vision and Diffusion Transformers
> 论文: https://arxiv.org/abs/2509.25164 (The Hong Kong Polytechnic University & OPPO Research Institute)

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `attention/BinaryAttention.py` | 新增 BinaryAttention, BinaryChannelAttention, C3k2_BinaryAttention |
| `__init__.py` | 追加1条import |
| `tasks.py` | base_modules追加3个模块 |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | ~90 | 追加 `from .attention.BinaryAttention import *` |
| `tasks.py` | ~1171 | base_modules追加 BinaryAttention, BinaryChannelAttention, C3k2_BinaryAttention |

**BinaryAttention 模型参数**:
- YOLO26s-midfusion-BinaryAttention: 442 layers, 20,185,945 params, 38.21 GFLOPs
- vs 基线(SPPF+C2PSA): 约+5M params

**架构改动**:
- RGB分支层8: C3k2 → C3k2_BinaryAttention (P4层)
- RGB分支层10: C3k2 → C3k2_BinaryAttention (P5层)
- IR分支层18: C3k2 → C3k2_BinaryAttention (P4层)
- IR分支层20: C3k2 → C3k2_BinaryAttention (P5层)
- 原位替换，无层号位移

**核心创新**:
- 1-bit二值化Q和K，用位运算(XNOR+popcount)替代浮点点积
- 可学习偏置减轻量化信息损失
- 8-bit量化注意力系数和value实现端到端加速
- 理论证明二值注意力保留原始协方差结构

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| BinaryAttention | ✅ 纳入 | CVPR2026创新性强，1-bit量化首次应用于视觉注意力，纯PyTorch实现 |

### 新增 AFFN 频域自相关融合模块

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-27/frequency/`
> 来源: 用户提供 (频域自相关融合网络)

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `frequency/AFFN.py` | 新增 AFFN, C2PSA_AFFN |
| `__init__.py` | 追加1条import |
| `tasks.py` | base_modules追加2个模块 |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | ~93 | 追加 `from .frequency.AFFN import *` |
| `tasks.py` | ~1175 | base_modules追加 AFFN, C2PSA_AFFN |

**AFFN 模型参数**:
- YOLO26s-midfusion-AFFN: 433 layers, 14,972,251 params, 34.20 GFLOPs
- vs 基线(SPPF+C2PSA): 约-3M params (更轻量)

**架构改动**:
- 层25: C2PSA → C2PSA_AFFN (FFT自相关功率谱增强)
- 原位替换，无层号位移

**核心创新**:
- FFT频域变换提取频域特征
- 自相关功率谱计算: R(k) = Xf(k) * conj(Xf(k))
- 频域增强: Xf_new = Xf + α * power
- 空间域增强: x_out = x_out + β * R
- α/β可学习参数自适应融合权重

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| AFFN | ✅ 纳入 | 频域自相关融合创新，纯PyTorch，适合作为C2PSA替代 |

### 新增 Flickerformer模块集合 (PAM, FSAS, SCAM, PhaseGuidedFilter)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-27/flickerformer/`
> 来源: Flickerformer (视频去闪烁网络)

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `frequency/Flickerformer.py` | 新增 PAM, PhaseGuidedFilter, FSAS, SCAM, C2PSA_PAM, C2PSA_FSAS, C2PSA_SCAM |
| `__init__.py` | 追加1条import |
| `tasks.py` | base_modules追加7个模块, 多输入分支追加1个 |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | ~96 | 追加 `from .frequency.Flickerformer import *` |
| `tasks.py` | ~1180 | base_modules追加 PAM, PhaseGuidedFilter, FSAS, SCAM, C2PSA_PAM, C2PSA_FSAS, C2PSA_SCAM |
| `tasks.py` | ~1240 | 多输入分支追加 PhaseGuidedFilter |

**模型参数**:
| 模块 | 层数 | 参数量 | GFLOPs |
|------|------|--------|--------|
| PAM | 435 | 15,033,945 | 34.24 |
| FSAS | 435 | 15,040,601 | 34.25 |
| SCAM | 517 | 25,881,721 | 42.92 |
| PhaseGuidedFilter | 482 | 158,703,036 | 417.72 |

**架构改动**:
- PAM: 层25 C2PSA → C2PSA_PAM (相位注意力)
- FSAS: 层25 C2PSA → C2PSA_FSAS (频域-空间自注意力)
- SCAM: 层25 C2PSA → C2PSA_SCAM (三路双向交叉注意力)
- PhaseGuidedFilter: 层21/22/23 Concat → PhaseGuidedFilter (相位引导融合)

**核心创新**:
- **PAM**: 只处理相位信息，保留幅度不变（相位包含结构信息）
- **FSAS**: QK在频域计算，V在空间域调制，O(N)复杂度
- **SCAM**: 三路分割+双向交叉注意力+频域FFN，信息流更丰富
- **PhaseGuidedFilter**: 利用频域相位相关性进行跨特征引导，适合RGBT融合

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| PAM | ✅ 纳入 | 相位注意力，理论支撑强，轻量 |
| FSAS | ✅ 纳入 | 频域QK交互，创新性强 |
| SCAM | ✅ 纳入 | 双向交叉注意力，信息流丰富 |
| PhaseGuidedFilter | ⚠️ 纳入[参数量大] | 相位引导滤波，适合RGBT融合，但参数量较大 |

### 新增 PolyNeXt模块集合 (PolyAttention, PolyConv)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-30/polynext/`
> 来源: PolyNeXt (ICLR2024 多项式神经网络)

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `attention/PolyNeXt.py` | 新增 PolyAttention, PolyConv, PolyMLP, C2PSA_PolyAttention, C3k2_PolyConv |
| `__init__.py` | 追加1条import |
| `tasks.py` | base_modules追加5个模块 |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | ~99 | 追加 `from .attention.PolyNeXt import *` |
| `tasks.py` | ~1185 | base_modules追加 PolyAttention, PolyConv, PolyMLP, C2PSA_PolyAttention, C3k2_PolyConv |

**模型参数**:
| 模块 | 层数 | 参数量 | GFLOPs |
|------|------|--------|--------|
| PolyAttention | 493 | 16,483,225 | 35.40 |
| PolyConv | 406 | 17,001,369 | 35.31 |

**架构改动**:
- PolyAttention: 层25 C2PSA → C2PSA_PolyAttention (多项式注意力)
- PolyConv: RGB分支层8/10, IR分支层18/20 C3k2 → C3k2_PolyConv (多项式卷积)

**核心创新**:
- **PolyAttention**: `((q @ k.T) * scale + 1) ** 4` 替代softmax，多项式近似更高效，无需exp操作
- **PolyConv**: `conv1(x) * conv2(x).flip(dims=[1])`，多项式交互增强特征表达，通道翻转实现跨通道交互
- **PolyMLP**: `high * low` 门控交互，分割+元素级乘法

**理论支撑**:
- 多项式可以近似任意连续函数（Stone-Weierstrass定理）
- 多项式注意力避免了exp操作，计算更友好
- 多项式卷积通过元素级乘法和通道翻转实现高阶特征交互

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| PolyAttention | ✅ 纳入 | ICLR2024创新，多项式近似softmax，计算高效 |
| PolyConv | ✅ 纳入 | 多项式交互，增强特征表达，纯PyTorch |

---

## 2026-06-30

### 新增 WDAM 小波双注意力模块

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-30/frequency/`
> 来源: wechat.md 中的 WDAM 模块

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| WDAM | ✅ 纳入 | 小波域窗口注意力，与FDConv/vHeat互补，针对RGBT频域融合 |

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `yolo26-RGBT-midfusion-WDAM.yaml` | 新建，层25 C2PSA → C2PSA_WDAM |
| `ultralytics/nn/ExtraModules/frequency/WDAM.py` | 新建，WDAM + C2PSA_WDAM 实现 |
| `ultralytics/nn/ExtraModules/__init__.py` | 追加 `from .frequency.WDAM import *` |
| `ultralytics/nn/tasks.py` | 追加 WDAM, C2PSA_WDAM 到 base_modules |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | 104行后 | 追加 WDAM 导入 |
| `tasks.py` | 1200行前 | 追加 WDAM, C2PSA_WDAM 到 base_modules |

**WDAM 模型参数**:
- YOLO26s-midfusion-WDAM: 444 layers, 18,120,937 params, 36.71 GFLOPs
- vs 基线 C2PSA: 参数量增加约 3M (窗口注意力+相对位置偏置表)

---

## 2026-07-03

### 新增 C2Former 跨模态可变形交叉注意力融合模块

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-07-03/fusion/`
> 来源: C2Former (旋转目标检测中的双模态融合模块)

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| C2Former_Fusion | ✅ 纳入 | 双向可变形交叉注意力，专为RGBT双模态设计 |
| C2PSA_C2Former | ✅ 纳入 | C2PSA包装器，可在瓶颈层引入跨模态交互 |

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `fusion/C2Former.py` | 新建，C2Former_Fusion + C2PSA_C2Former + ModalityNorm 实现 |
| `__init__.py` | 追加1条import |
| `tasks.py` | base_modules追加2个模块 + 多输入分支追加C2Former_Fusion |
| `yolo26-RGBT-midfusion-C2Former.yaml` | 新建，层21/22/23 Concat → C2Former_Fusion |
| `yolo26-RGBT-midfusion-C2PSA_C2Former.yaml` | 新建，层25 C2PSA → C2PSA_C2Former |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | ~130 | 追加 `from .fusion.C2Former import *` |
| `tasks.py` | ~1205 | base_modules追加 C2Former_Fusion, C2PSA_C2Former |
| `tasks.py` | ~1255 | 多输入分支追加 C2Former_Fusion |

**架构改动**:
- C2Former_Fusion: 层21/22/23 Concat → C2Former_Fusion (双向可变形交叉注意力)
- C2PSA_C2Former: 层25 C2PSA → C2PSA_C2Former (跨模态交叉注意力)

**核心创新**:
- **可变形交叉注意力**: 使用 offset 机制实现可变形采样，自适应对齐两个模态的特征位置
- **双向交叉注意力**: VIS 查询 IR 特征，IR 查询 VIS 特征，实现真正的跨模态交互
- **ModalityNorm**: 用源模态统计量重缩放参考模态，处理 VIS 和 IR 模态间的分布差异
- **残差交叉连接**: vis_x += out_lwir, lwir_x += out_vis，增强跨模态信息流动

**与已有融合模块的区别**:
| 模块 | 机制 | 特点 |
|------|------|------|
| CIFusion/AFF/iAFF | 简单加权融合 | 轻量但交互有限 |
| ICAFusion | 标准交叉注意力 | 无位置自适应 |
| **C2Former** | **可变形交叉注意力+ModalityNorm** | **自适应跨模态对齐** |

## 2026-06-30

### 新增 SPDConv + CSPOmniKernel 模块 (来自 GeoFuse-YOLO)

> 配置目录: `ultralytics/cfg/models/26-RGBT/2026-06-30/conv/`
> 来源: GeoFuse-YOLO (SOEP: Small Object Enhance Pyramid)

**筛选分析**:
| 模块 | 决定 | 原因 |
|------|------|------|
| SPDConv | ✅ 纳入 | 空间到深度转换+残差, 小目标增强, 纯PyTorch, 与现有模块互补 |
| CSPOmniKernel | ✅ 纳入 | CSP+多尺度全方向卷积+频域门控, 纯PyTorch, 轻量设计 |
| MCFGatedFusion | ❌ 跳过 | 代码质量差(forward内动态创建Conv), 机制与现有融合模块重叠 |
| GSConvE | ❌ 跳过 | 与已有GSConv机制重叠, 增量改进不明显 |
| SNI | ❌ 跳过 | 过于简单(alpha*upsample), 与CARAFE_Upsample重叠 |
| FGM | ❌ 跳过 | 过于简单, 与现有SE类注意力重叠 |
| contrast.py | ❌ 跳过 | 训练工具非网络模块, 需修改核心源码 |
| filling/router | ❌ 跳过 | 基础设施代码非即插即用模块 |

**文件修改对照表**:
| 配置/代码 | 改动 |
|-----------|------|
| `yolo26-RGBT-midfusion-GeoFuse.yaml` | 新建, 层4/14 C3k2→SPDConv, 层6/16 C3k2→CSPOmniKernel |
| `ultralytics/nn/ExtraModules/conv/SPDConv.py` | 新建, SPDConv 实现 (73行) |
| `ultralytics/nn/ExtraModules/conv/CSPOmniKernel.py` | 新建, CSPOmniKernel+OmniKernel+FGM 实现 (177行) |
| `ultralytics/nn/ExtraModules/__init__.py` | 追加 `from .conv.SPDConv import *` 和 `from .conv.CSPOmniKernel import *` |
| `ultralytics/nn/tasks.py` | 追加 SPDConv, CSPOmniKernel 到 base_modules |

**代码文件修改表**:
| 文件 | 行号 | 改动 |
|------|------|------|
| `__init__.py` | 122行后 | 追加 SPDConv, CSPOmniKernel 导入 |
| `tasks.py` | 1204行前 | 追加 SPDConv, CSPOmniKernel 到 base_modules |
| `SPDConv.py` | 新建 | 空间到深度卷积模块 |
| `CSPOmniKernel.py` | 新建 | CSP多尺度全方向卷积块 |

**替换说明** (原位替换, 无层号位移):
| 层号 | 原内容 | 新内容 | 位置 |
|------|--------|--------|------|
| 4 | `C3k2, [256, False, 0.25]` (n=2) | `SPDConv, [256]` (n=1) | RGB-P2 |
| 6 | `C3k2, [512, False, 0.25]` (n=2) | `CSPOmniKernel, [512]` (n=1) | RGB-P3 |
| 14 | `C3k2, [256, False, 0.25]` (n=2) | `SPDConv, [256]` (n=1) | IR-P2 |
| 16 | `C3k2, [512, False, 0.25]` (n=2) | `CSPOmniKernel, [512]` (n=1) | IR-P3 |
