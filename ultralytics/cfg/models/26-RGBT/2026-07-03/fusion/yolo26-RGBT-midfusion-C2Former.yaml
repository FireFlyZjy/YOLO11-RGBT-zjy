# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# C2Former_Fusion: 跨模态可变形交叉注意力融合
# 改动: 层21/22/23 Concat → C2Former_Fusion (双向可变形交叉注意力)
# 原位替换, 无层号位移
#
# C2Former 核心机制:
#   1. 可变形交叉注意力: 使用 offset 机制实现可变形采样
#   2. 双向交叉注意力: VIS 查询 IR 特征，IR 查询 VIS 特征
#   3. ModalityNorm: 用源模态统计量重缩放参考模态
#   4. 残差连接: vis_x += out_lwir, lwir_x += out_vis
#
# 对 RGBT 的价值:
#   - 可变形采样自适应对齐两个模态的特征位置
#   - 双向交叉注意力实现真正的跨模态交互
#   - ModalityNorm 处理 VIS 和 IR 模态间的分布差异
#
# 来源: C2Former (旋转目标检测中的双模态融合模块)

# Parameters
nc: 3 # number of classes
scales:
  s: [0.50, 0.50, 1024]

ch: 4
# YOLO26s backbone (RGB + IR mid-fusion)
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Silence, []]  # 0-P1/2
    # visible
  - [0, 1, SilenceChannel, [0,3]]  # 1-P1/2
  - [-1, 1, Conv, [64, 3, 2]] # 2-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 3-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 5-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 7-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 9-P5/32
  - [-1, 2, C3k2, [1024, True, 0.5, True]] # 10

  # infrared
  - [ 0, 1, SilenceChannel, [ 3,4 ] ]  # 11-P1/2
  - [ -1, 1, Conv, [ 64, 3, 2 ] ] # 12-P1/2
  - [ -1, 1, Conv, [ 128, 3, 2 ] ] # 13-P2/4
  - [ -1, 2, C3k2, [ 256, False, 0.25 ] ]
  - [ -1, 1, Conv, [ 256, 3, 2 ] ] # 15-P3/8
  - [ -1, 2, C3k2, [ 512, False, 0.25 ] ]
  - [ -1, 1, Conv, [ 512, 3, 2 ] ] # 17-P4/16
  - [ -1, 2, C3k2, [ 512, True ] ]
  - [ -1, 1, Conv, [ 1024, 3, 2 ] ] # 19-P5/32
  - [ -1, 2, C3k2, [ 1024, True , 0.5, True] ]  #20
  - [[6, 16], 1, C2Former_Fusion, [256, 8, 1, 2]]  # P3 融合 21 (原 Concat → C2Former_Fusion)
  - [[8, 18], 1, C2Former_Fusion, [512, 8, 1, 2]]  # P4 融合 22 (原 Concat → C2Former_Fusion)
  - [[10, 20], 1, C2Former_Fusion, [1024, 8, 1, 2]]  # P5 融合 23 (原 Concat → C2Former_Fusion)

  - [ -1, 1, SPPF, [ 1024, 5, 3, True ] ] # 24
  - [ -1, 1, C2PSA, [ 1024 ] ] # 25

# YOLO26s head
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 22], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2, [512, False]] # 28

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 21], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2, [256, False]] # 31 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 28], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2, [512, False]] # 34 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 25], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2, [1024, True, 0.5, True]] # 37 (P5/32-large)

  - [[31, 34, 37], 1, Detect, [nc]] # Detect(P3, P4, P5)
