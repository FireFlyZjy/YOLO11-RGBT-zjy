# KAIST dataset (RGBT 双模态, 仅 person 一类)
# train/val 指向可见光 images/visible 目录
# 训练时 IR 配对: pairs_rgb_ir=['visible','infrared']  (默认规则, 将 visible 替换为 infrared 得到红外目录)
# 注意: 需要先将 KAIST 数据目录重组为 images/visible + images/infrared 结构 (与 FLIR/LLVIP 一致, 见 README 方法3)

train: /home/lyh/zjy/dataset/KAIST/images/visible/train # 7601 images
val: /home/lyh/zjy/dataset/KAIST/images/visible/val # 2252 images

# number of classes
nc: 1

# class names
names: ["person"]
