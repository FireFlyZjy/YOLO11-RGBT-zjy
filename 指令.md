# 加模块任务要求汇总

## 总体目标
在我已有的 YOLO 双模态（RGB + Infrared）目标检测代码中，从外部目录中筛选有涨点潜力的算法模块，集成为 YOLO 兼容的包装器，统一管理并验证。

## 外部模块来源（按处理顺序）
1. `cv-attention/` — 32 个注意力机制脚本
2. `yolo-improve/` — 含 AIFI、ScalSeq 等改进模块
3. `Plug-and-play module/` — 28 个模块（注意力、卷积、融合等）
4. `CMFADet-main/` — RGB-Infrared 双模态检测论文代码，4 核心模块 + ATAH 检测头
5. `ICAFusion-main/` — 交叉注意力Transformer融合论文代码

## 操作要求

### 1. 筛选标准
- 跳过的模块：
  - 与已有模块功能重叠的
  - 太重（如 O(n²) 注意力、完整 Transformer）
  - 需要自定义 CUDA 扩展
  - 源码有 bug
  - 侵入性强（需修改 Ultralytics 核心损失/训练流程）
- 纳入的模块：
  - 机制新颖且与现有模块互补
  - 轻量级可插入 YOLO
  - 对双模态检测有针对性（如特征融合、IR 专用、频域处理）

### 2. 代码组织规范
- **核心算法 → 独立文件**: 每个模块的核心实现放在独立 `.py` 文件中
- **包装器 → common.py**: 所有 YOLO 适配代码（接受 c1/c2 参数、通道对齐）放在 common.py 中作为薄包装器
- 按功能分类到子目录：
  - `attention/` — 注意力机制
  - `conv/` — 卷积增强
  - `fusion/` — 特征融合
  - `head/` — 检测头 (ATAH)
- 注册到 `tasks.py` 的 `base_modules` 和 `repeat_modules`（若需要 n 参数）
- 多输入模块（from 为列表）需要在 parse_model 中特殊处理

### 3. YAML 配置规范
- 每个模块创建一个 YAML 配置文件
- 基于 `yolo26-RGBT-midfusion.yaml` 模板修改
- 插入策略：在 P3/P4/P5 双模态 Concat 融合之后插入注意力模块
- 按日期分目录存放（如 `2026-05-13/`, `2026-05-14/`, `2026-05-19/`）
- YAML 头部注释写明与基线模板的差异（改了哪行，什么变化）

### 4. 文档规范

#### 创新点.md
- 每次改进在文档末尾追加
- 同日改进不加新日期头，不同日改进先加 `## YYYY-MM-DD` 再追加
- 内容应包含：新增模块名称、来源论文、核心机制说明、参数量、插入策略、改进动机
- 需附验证结果（是否通过实例化和 forward pass）
- YAML 配置文件需列出并说明与基线的差异

#### 改进位置.md
- 每次改进在文档末尾追加
- 同日改进不加新日期头，不同日改进先加 `## YYYY-MM-DD` 再追加
- 内容应包含：配置目录、文件修改对照表（配置文件 vs 模块 vs 改动）、具体改动行号
- 层号位移对照表（Backbone 和 Head 分开，标明 原Layer → 新Layer）
- 代码文件修改表（哪些文件改了哪些内容）
- 架构示意图（ASCII 图展示模块插入位置）
- 若涉及筛选，需附筛选结果表（纳入/跳过及原因）

### 5. 验证要求
- 每个新模块必须通过模型实例化和 forward pass 测试
- 不允许破坏已有模块（回归测试）

### 6. 消融实验 YAML 规范
- 每类实验 YAML 统一放在独立子目录，命名格式 `Module=Component1+Component2+...`
- 所有 YAML 层号完全一致（替换模块不增减层），确保对比严谨
- Baseline 作为无模块对照组，其余逐模块叠加
- 每个 YAML 头部注明改动（哪个模块、什么变化）

## 最终纳入模块清单（共 26 个）

### head/ (1 个)
| 模块 | 来源 | 日期 |
|------|------|------|
| Detect_ATAH | CMFADet | 05-26 |

### attention/ (16 个)
| 模块 | 来源 | 日期 |
|------|------|------|
| Att_CBAM | cv-attention | 05-13 |
| Att_Coord | cv-attention | 05-13 |
| Att_SimAM | cv-attention | 05-13 |
| Att_CPCA | cv-attention | 05-13 |
| Att_EMA | cv-attention | 05-13 |
| Att_ECA | cv-attention | 05-13 |
| Att_Shuffle | cv-attention | 05-13 |
| Att_LSKA | cv-attention | 05-13 |
| Att_Triplet | cv-attention | 05-13 |
| Att_GAM | cv-attention | 05-13 |
| Att_ELA | cv-attention | 05-13 |
| Att_AIFI | yolo-improve | 05-14 |
| Att_ULSAM | Plug-and-play | 05-14 |
| Att_StripPool | Plug-and-play | 05-14 |
| Att_SFEM | CMFADet | 05-19 |
| Att_IRAFAB | CMFADet | 05-19 |

### conv/ (1 个)
| 模块 | 来源 | 日期 |
|------|------|------|
| Conv_DE | CMFADet | 05-19 |

### fusion/ (5 个)
| 模块 | 来源 | 日期 |
|------|------|------|
| Att_ScalSeq | yolo-improve | 05-14 |
| Att_AFF | Plug-and-play | 05-14 |
| Att_iAFF | Plug-and-play | 05-14 |
| Att_CIFusion | CMFADet | 05-19 |
| Att_ICAFusion | ICAFusion (PR 2023) | 05-19 |

### common.py（保留的原包装器，6 个）
| 类 | 用途 |
|----|------|
| GCBlock | GatingContext 包装器 |
| Conv_Blaze | BlazeBlock 包装器 |
| Conv_AC | ACBlock 包装器 |
| Conv_SE | SEAttention + Conv 组合 |
| CSP_ATT | CSP + SPP + SE 组合 |
| C2f_DCN | DCNv2 瓶颈的 C2f |

### head/ (1 个)
| 模块 | 用途 |
|------|------|
| Detect_ATAH | 自适应任务感知对齐检测头，替代标准Detect |

## 关键用户反馈（需牢记）
1. 不要把所有实现塞进 common.py，每个模块应有独立文件
2. 新模块按功能分类子目录存放
3. YAML 配置文件按日期分目录
4. 同步更新 创新点.md 和 改进位置.md
5. 消融实验 YAML 统一放子目录，层号完全一致确保对比严谨
